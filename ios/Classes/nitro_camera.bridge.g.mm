// Generated by Nitrogen Modules. Do not edit.
// nitro_generator: 0.5.9
// Generated from: nitro_camera.native.dart
#include <stdint.h>
#include <stdbool.h>
#include <string.h>
#include <stdlib.h>
#if defined(__APPLE__)
#import <Foundation/Foundation.h>
#endif
#if defined(__ANDROID__) || defined(__linux__)
#include <dlfcn.h>
#endif
#include "dart_api_dl.h"
#include "nitro_camera.bridge.g.h"

#if defined(_MSC_VER) && !defined(strdup)
#define strdup _strdup
#endif

extern "C" {
NITRO_EXPORT uint32_t nitro_camera_nitro_abi_version(void) {
    return 1;
}
NITRO_EXPORT const char* nitro_camera_nitro_bridge_checksum(void) {
    return "d26cbaf5f438b87b";
}
NITRO_EXPORT intptr_t nitro_camera_init_dart_api_dl(void* data) {
    return Dart_InitializeApiDL(data);
}
}
static thread_local NitroError g_nitro_error = { 0, nullptr, nullptr, nullptr, nullptr };

extern "C" {
NitroError* nitro_camera_get_error() { return &g_nitro_error; }
void nitro_camera_clear_error() {
    g_nitro_error.hasError = 0;
    if (g_nitro_error.name) { free((void*)g_nitro_error.name); g_nitro_error.name = nullptr; }
    if (g_nitro_error.message) { free((void*)g_nitro_error.message); g_nitro_error.message = nullptr; }
    if (g_nitro_error.code) { free((void*)g_nitro_error.code); g_nitro_error.code = nullptr; }
    if (g_nitro_error.stackTrace) { free((void*)g_nitro_error.stackTrace); g_nitro_error.stackTrace = nullptr; }
}

static void nitro_report_error(const char* name, const char* message, const char* code, const char* stack) {
    nitro_camera_clear_error();
    g_nitro_error.hasError = 1;
    g_nitro_error.name = name ? strdup(name) : strdup("NativeException");
    g_nitro_error.message = message ? strdup(message) : strdup("An unknown native exception occurred.");
    g_nitro_error.code = code ? strdup(code) : nullptr;
    g_nitro_error.stackTrace = stack ? strdup(stack) : nullptr;
}
}

extern "C" {
#ifdef __ANDROID__
// Defined in the JNI section below: erases the g_zero_copy_refs pin for
// [ptr] and deletes the JNI global ref (see the JNI prologue).
void nitro_camera_zero_copy_release(void* ptr);
#endif
void nitro_camera_release_CameraFrame(void* ptr) {
    if (!ptr) { return; }
#ifdef __ANDROID__
    nitro_camera_zero_copy_release(ptr);
#endif
    free(ptr);
}
void nitro_camera_release_CameraConfig(void* ptr) {
    if (!ptr) { return; }
    free(ptr);
}
void nitro_camera_release_ResolvedConfig(void* ptr) {
    if (!ptr) { return; }
    free(ptr);
}
void nitro_camera_release_PhotoOptions(void* ptr) {
    if (!ptr) { return; }
    free(ptr);
}
void nitro_camera_release_RecordingOptions(void* ptr) {
    if (!ptr) { return; }
    free(ptr);
}
}

#if defined(__ANDROID__) || defined(__linux__)
static void* _nitro_camera_lib_h = nullptr;
#endif

extern "C" {
// Finding 1: promotes this native library to process-wide visibility so that
// @Native<F>() Dart bindings resolve via DynamicLibrary.process() on Android/Linux.
// On iOS/macOS this is a no-op — symbols are already in the process namespace
// via static linking.  Call once from the Kotlin/Swift plugin init.
NITRO_EXPORT void nitro_camera_enable_native_bindings(void) {
#if defined(__ANDROID__) || defined(__linux__)
    if (!_nitro_camera_lib_h) {
        Dl_info info;
        if (dladdr((void*)nitro_camera_enable_native_bindings, &info) && info.dli_fname) {
            // RTLD_GLOBAL | RTLD_NOLOAD promotes already-loaded lib to global namespace
            // without re-loading it, making symbols available to DynamicLibrary.process().
            _nitro_camera_lib_h = dlopen(info.dli_fname, RTLD_LAZY | RTLD_GLOBAL | RTLD_NOLOAD);
        }
    }
#endif
}
}

#ifdef __ANDROID__
#include <jni.h>
#include <android/log.h>
#define LOGE(...) __android_log_print(ANDROID_LOG_ERROR, "Nitrogen", __VA_ARGS__)

static JavaVM* g_jvm = nullptr;
static jclass g_bridgeClass = nullptr;

// ── Cached JNI IDs (initialized once in JNI_OnLoad, safe to use from any thread) ──
static jmethodID g_exc_getName = nullptr;
static jmethodID g_exc_getMessage = nullptr;
static jmethodID g_mid_create_instance_call = nullptr;
static jmethodID g_mid_destroy_instance_call = nullptr;
static jmethodID g_mid_requestCameraPermission_call = nullptr;
static jmethodID g_mid_getCameraPermissionStatus_call = nullptr;
static jmethodID g_mid_requestMicrophonePermission_call = nullptr;
static jmethodID g_mid_getMicrophonePermissionStatus_call = nullptr;
static jmethodID g_mid_getAvailableCameraDevicesJson_call = nullptr;
static jmethodID g_mid_getAvailableCameraDevices_call = nullptr;
static jmethodID g_mid_getDeviceCount_call = nullptr;
static jmethodID g_mid_getDevice_call = nullptr;
static jmethodID g_mid_openCamera_call = nullptr;
static jmethodID g_mid_closeCamera_call = nullptr;
static jmethodID g_mid_startPreview_call = nullptr;
static jmethodID g_mid_stopPreview_call = nullptr;
static jmethodID g_mid_setZoom_call = nullptr;
static jmethodID g_mid_setFocusPoint_call = nullptr;
static jmethodID g_mid_setAutoFocus_call = nullptr;
static jmethodID g_mid_setExposure_call = nullptr;
static jmethodID g_mid_setFlash_call = nullptr;
static jmethodID g_mid_setTorch_call = nullptr;
static jmethodID g_mid_setWhiteBalance_call = nullptr;
static jmethodID g_mid_setHdr_call = nullptr;
static jmethodID g_mid_takePhoto_call = nullptr;
static jmethodID g_mid_startVideoRecording_call = nullptr;
static jmethodID g_mid_stopVideoRecording_call = nullptr;
static jmethodID g_mid_pauseRecording_call = nullptr;
static jmethodID g_mid_resumeRecording_call = nullptr;
static jmethodID g_mid_cancelRecording_call = nullptr;
static jmethodID g_mid_enableFrameProcessing_call = nullptr;
static jmethodID g_mid_setFrameFormat_call = nullptr;
static jmethodID g_mid_setSamplingRate_call = nullptr;
static jmethodID g_mid_setFilterShader_call = nullptr;
static jmethodID g_mid_updateOverlay_call = nullptr;
static jmethodID g_mid_configure_call = nullptr;
static jmethodID g_mid_getSessionStateJson_call = nullptr;
static jmethodID g_mid_setVideoStabilization_call = nullptr;
static jmethodID g_mid_setLowLightBoost_call = nullptr;
static jmethodID g_mid_setTorchLevel_call = nullptr;
static jmethodID g_mid_lockExposure_call = nullptr;
static jmethodID g_mid_lockFocus_call = nullptr;
static jmethodID g_mid_lockWhiteBalance_call = nullptr;
static jmethodID g_mid_setTargetOrientation_call = nullptr;
static jmethodID g_mid_setDistortionCorrection_call = nullptr;
static jmethodID g_mid_enableOrientationEvents_call = nullptr;
static jmethodID g_mid_enableDeviceAvailabilityEvents_call = nullptr;
static jmethodID g_mid_setNativeDetector_call = nullptr;
static jmethodID g_mid_getConcurrentCameraIdsJson_call = nullptr;
static jmethodID g_mid_takePhotoWithOptions_call = nullptr;
static jmethodID g_mid_takeSnapshot_call = nullptr;
static jmethodID g_mid_reset_call = nullptr;
static jmethodID g_mid_nitro_camera_register_frame_stream_stream_call = nullptr;
static jmethodID g_mid_nitro_camera_release_frame_stream_stream_call = nullptr;
static jmethodID g_mid_nitro_camera_register_event_stream_stream_call = nullptr;
static jmethodID g_mid_nitro_camera_release_event_stream_stream_call = nullptr;
static jclass g_cls_CameraEvent = nullptr;
static jmethodID g_mid_CameraEvent_encode = nullptr;
static jclass g_cls_CameraFrame = nullptr;
static jmethodID g_ctor_CameraFrame = nullptr;
static jfieldID g_fid_CameraFrame_pixels = nullptr;
static jfieldID g_fid_CameraFrame_size = nullptr;
static jfieldID g_fid_CameraFrame_width = nullptr;
static jfieldID g_fid_CameraFrame_height = nullptr;
static jfieldID g_fid_CameraFrame_timestamp = nullptr;
static jfieldID g_fid_CameraFrame_orientation = nullptr;
static jfieldID g_fid_CameraFrame_textureId = nullptr;
static jfieldID g_fid_CameraFrame_bytesPerRow = nullptr;
static jfieldID g_fid_CameraFrame_pixelFormat = nullptr;
static jfieldID g_fid_CameraFrame_isMirrored = nullptr;
static jclass g_cls_CameraConfig = nullptr;
static jmethodID g_ctor_CameraConfig = nullptr;
static jfieldID g_fid_CameraConfig_zoom = nullptr;
static jfieldID g_fid_CameraConfig_exposure = nullptr;
static jfieldID g_fid_CameraConfig_flash = nullptr;
static jfieldID g_fid_CameraConfig_torch = nullptr;
static jfieldID g_fid_CameraConfig_torchLevel = nullptr;
static jfieldID g_fid_CameraConfig_whiteBalanceKelvin = nullptr;
static jfieldID g_fid_CameraConfig_videoHdr = nullptr;
static jfieldID g_fid_CameraConfig_lowLightBoost = nullptr;
static jfieldID g_fid_CameraConfig_autoFocus = nullptr;
static jfieldID g_fid_CameraConfig_videoStabilization = nullptr;
static jfieldID g_fid_CameraConfig_active = nullptr;
static jfieldID g_fid_CameraConfig_enableFrameProcessing = nullptr;
static jfieldID g_fid_CameraConfig_pixelFormat = nullptr;
static jfieldID g_fid_CameraConfig_samplingRate = nullptr;
static jclass g_cls_ResolvedConfig = nullptr;
static jmethodID g_ctor_ResolvedConfig = nullptr;
static jfieldID g_fid_ResolvedConfig_width = nullptr;
static jfieldID g_fid_ResolvedConfig_height = nullptr;
static jfieldID g_fid_ResolvedConfig_fps = nullptr;
static jfieldID g_fid_ResolvedConfig_pixelFormat = nullptr;
static jfieldID g_fid_ResolvedConfig_videoHdrEnabled = nullptr;
static jfieldID g_fid_ResolvedConfig_autoFocusSystem = nullptr;
static jfieldID g_fid_ResolvedConfig_active = nullptr;
static jclass g_cls_PhotoOptions = nullptr;
static jmethodID g_ctor_PhotoOptions = nullptr;
static jfieldID g_fid_PhotoOptions_flash = nullptr;
static jfieldID g_fid_PhotoOptions_qualityPrioritization = nullptr;
static jfieldID g_fid_PhotoOptions_enableShutterSound = nullptr;
static jfieldID g_fid_PhotoOptions_skipMetadata = nullptr;
static jfieldID g_fid_PhotoOptions_enableAutoRedEyeReduction = nullptr;
static jfieldID g_fid_PhotoOptions_latitude = nullptr;
static jfieldID g_fid_PhotoOptions_longitude = nullptr;
static jfieldID g_fid_PhotoOptions_altitude = nullptr;
static jfieldID g_fid_PhotoOptions_hasLocation = nullptr;
static jfieldID g_fid_PhotoOptions_outputFormat = nullptr;
static jclass g_cls_RecordingOptions = nullptr;
static jmethodID g_ctor_RecordingOptions = nullptr;
static jfieldID g_fid_RecordingOptions_codec = nullptr;
static jfieldID g_fid_RecordingOptions_fileType = nullptr;
static jfieldID g_fid_RecordingOptions_bitRate = nullptr;
static jfieldID g_fid_RecordingOptions_maxDurationMs = nullptr;
static jfieldID g_fid_RecordingOptions_maxFileSizeBytes = nullptr;
static jfieldID g_fid_RecordingOptions_latitude = nullptr;
static jfieldID g_fid_RecordingOptions_longitude = nullptr;
static jfieldID g_fid_RecordingOptions_altitude = nullptr;
static jfieldID g_fid_RecordingOptions_hasLocation = nullptr;

#include <unordered_map>
#include <mutex>
// GlobalRef map: keeps zero-copy struct backing objects alive until Dart finalizer fires.
static std::unordered_map<void*, jobject> g_zero_copy_refs;
static std::mutex g_zero_copy_refs_mtx;


// RAII guard: auto-detaches a thread from the JVM when it exits.
// One instance is stored in thread-local storage; its destructor fires
// when the thread terminates, ensuring no JVM thread descriptor leaks.
struct NitroJniThreadGuard {
    bool attached = false;
    ~NitroJniThreadGuard() {
        if (attached && g_jvm != nullptr) {
            g_jvm->DetachCurrentThread();
        }
    }
};
static thread_local NitroJniThreadGuard g_thread_guard;

static void nitro_report_jni_exception(JNIEnv* env, jthrowable ex, NitroError* _nitro_err) {
    // MUST clear the pending exception before making any further JNI calls.
    // JNI aborts if any JNI function (e.g. GetObjectClass) is called while
    // an exception is still pending.
    env->ExceptionClear();
    jclass ex_class = env->GetObjectClass(ex);
    jstring j_name = (jstring)env->CallObjectMethod(ex_class, g_exc_getName);
    const char* name = (j_name != nullptr) ? env->GetStringUTFChars(j_name, 0) : "JavaException";

    jstring j_msg = (jstring)env->CallObjectMethod(ex, g_exc_getMessage);
    const char* msg = (j_msg != nullptr) ? env->GetStringUTFChars(j_msg, 0) : "No message provided";

    // S8: write to out-param slot (sync) or TLS slot (async, _nitro_err == nullptr).
    if (_nitro_err) {
        _nitro_err->hasError = 1;
        _nitro_err->name       = strdup(name);
        _nitro_err->message    = strdup(msg);
        _nitro_err->code       = nullptr;
        _nitro_err->stackTrace = nullptr;
    } else {
        // Async functions use TLS slot (read by callAsync via get_error/clear_error).
        nitro_report_error(name, msg, nullptr, nullptr);
    }

    if (j_name) {
        env->ReleaseStringUTFChars(j_name, name);
        env->DeleteLocalRef(j_name);
    }
    if (j_msg) {
        env->ReleaseStringUTFChars(j_msg, msg);
        env->DeleteLocalRef(j_msg);
    }
    env->DeleteLocalRef(ex_class);
    env->DeleteLocalRef(ex);
}

static CameraFrame pack_CameraFrame_from_jni(JNIEnv* env, jobject obj) {
    CameraFrame result;
    jobject buf_pixels = env->GetObjectField(obj, g_fid_CameraFrame_pixels);
    if (buf_pixels == nullptr) {
        jclass npe = env->FindClass("java/lang/NullPointerException");
        if (npe) env->ThrowNew(npe, "CameraFrame.pixels: TypedData ByteBuffer is null");
        return result;
    }
    result.pixels = (uint8_t*)env->GetDirectBufferAddress(buf_pixels);
    if (result.pixels == nullptr) {
        jclass iae = env->FindClass("java/lang/IllegalArgumentException");
        if (iae) env->ThrowNew(iae, "CameraFrame.pixels: @ZeroCopy requires ByteBuffer.allocateDirect() — heap-backed ByteBuffer.wrap() is not supported.");
        env->DeleteLocalRef(buf_pixels);
        return result;
    }
    env->DeleteLocalRef(buf_pixels);
    result.size = env->GetLongField(obj, g_fid_CameraFrame_size);
    result.width = env->GetLongField(obj, g_fid_CameraFrame_width);
    result.height = env->GetLongField(obj, g_fid_CameraFrame_height);
    result.timestamp = env->GetLongField(obj, g_fid_CameraFrame_timestamp);
    result.orientation = env->GetLongField(obj, g_fid_CameraFrame_orientation);
    result.textureId = env->GetLongField(obj, g_fid_CameraFrame_textureId);
    result.bytesPerRow = env->GetLongField(obj, g_fid_CameraFrame_bytesPerRow);
    result.pixelFormat = env->GetLongField(obj, g_fid_CameraFrame_pixelFormat);
    result.isMirrored = env->GetLongField(obj, g_fid_CameraFrame_isMirrored);
    return result;
}
static jobject unpack_CameraFrame_to_jni(JNIEnv* env, const CameraFrame* st) {
    if (st->pixels == nullptr) {
        jclass npe = env->FindClass("java/lang/NullPointerException");
        if (npe) env->ThrowNew(npe, "CameraFrame.pixels: TypedData pointer is null");
        return nullptr;
    }
    jobject dbuf_pixels = env->NewDirectByteBuffer((void*)st->pixels, st->size);
    jobject result = env->NewObject(g_cls_CameraFrame, g_ctor_CameraFrame, dbuf_pixels, (jlong)st->size, (jlong)st->width, (jlong)st->height, (jlong)st->timestamp, (jlong)st->orientation, (jlong)st->textureId, (jlong)st->bytesPerRow, (jlong)st->pixelFormat, (jlong)st->isMirrored);
    if (dbuf_pixels) env->DeleteLocalRef(dbuf_pixels);
    return result;
}
static CameraConfig pack_CameraConfig_from_jni(JNIEnv* env, jobject obj) {
    CameraConfig result;
    result.zoom = env->GetDoubleField(obj, g_fid_CameraConfig_zoom);
    result.exposure = env->GetDoubleField(obj, g_fid_CameraConfig_exposure);
    result.flash = env->GetLongField(obj, g_fid_CameraConfig_flash);
    result.torch = env->GetLongField(obj, g_fid_CameraConfig_torch);
    result.torchLevel = env->GetDoubleField(obj, g_fid_CameraConfig_torchLevel);
    result.whiteBalanceKelvin = env->GetLongField(obj, g_fid_CameraConfig_whiteBalanceKelvin);
    result.videoHdr = env->GetLongField(obj, g_fid_CameraConfig_videoHdr);
    result.lowLightBoost = env->GetLongField(obj, g_fid_CameraConfig_lowLightBoost);
    result.autoFocus = env->GetLongField(obj, g_fid_CameraConfig_autoFocus);
    result.videoStabilization = env->GetLongField(obj, g_fid_CameraConfig_videoStabilization);
    result.active = env->GetLongField(obj, g_fid_CameraConfig_active);
    result.enableFrameProcessing = env->GetLongField(obj, g_fid_CameraConfig_enableFrameProcessing);
    result.pixelFormat = env->GetLongField(obj, g_fid_CameraConfig_pixelFormat);
    result.samplingRate = env->GetLongField(obj, g_fid_CameraConfig_samplingRate);
    return result;
}
static jobject unpack_CameraConfig_to_jni(JNIEnv* env, const CameraConfig* st) {
    jobject result = env->NewObject(g_cls_CameraConfig, g_ctor_CameraConfig, (jdouble)st->zoom, (jdouble)st->exposure, (jlong)st->flash, (jlong)st->torch, (jdouble)st->torchLevel, (jlong)st->whiteBalanceKelvin, (jlong)st->videoHdr, (jlong)st->lowLightBoost, (jlong)st->autoFocus, (jlong)st->videoStabilization, (jlong)st->active, (jlong)st->enableFrameProcessing, (jlong)st->pixelFormat, (jlong)st->samplingRate);
    return result;
}
static ResolvedConfig pack_ResolvedConfig_from_jni(JNIEnv* env, jobject obj) {
    ResolvedConfig result;
    result.width = env->GetLongField(obj, g_fid_ResolvedConfig_width);
    result.height = env->GetLongField(obj, g_fid_ResolvedConfig_height);
    result.fps = env->GetLongField(obj, g_fid_ResolvedConfig_fps);
    result.pixelFormat = env->GetLongField(obj, g_fid_ResolvedConfig_pixelFormat);
    result.videoHdrEnabled = env->GetLongField(obj, g_fid_ResolvedConfig_videoHdrEnabled);
    result.autoFocusSystem = env->GetLongField(obj, g_fid_ResolvedConfig_autoFocusSystem);
    result.active = env->GetLongField(obj, g_fid_ResolvedConfig_active);
    return result;
}
static jobject unpack_ResolvedConfig_to_jni(JNIEnv* env, const ResolvedConfig* st) {
    jobject result = env->NewObject(g_cls_ResolvedConfig, g_ctor_ResolvedConfig, (jlong)st->width, (jlong)st->height, (jlong)st->fps, (jlong)st->pixelFormat, (jlong)st->videoHdrEnabled, (jlong)st->autoFocusSystem, (jlong)st->active);
    return result;
}
static PhotoOptions pack_PhotoOptions_from_jni(JNIEnv* env, jobject obj) {
    PhotoOptions result;
    result.flash = env->GetLongField(obj, g_fid_PhotoOptions_flash);
    result.qualityPrioritization = env->GetLongField(obj, g_fid_PhotoOptions_qualityPrioritization);
    result.enableShutterSound = env->GetLongField(obj, g_fid_PhotoOptions_enableShutterSound);
    result.skipMetadata = env->GetLongField(obj, g_fid_PhotoOptions_skipMetadata);
    result.enableAutoRedEyeReduction = env->GetLongField(obj, g_fid_PhotoOptions_enableAutoRedEyeReduction);
    result.latitude = env->GetDoubleField(obj, g_fid_PhotoOptions_latitude);
    result.longitude = env->GetDoubleField(obj, g_fid_PhotoOptions_longitude);
    result.altitude = env->GetDoubleField(obj, g_fid_PhotoOptions_altitude);
    result.hasLocation = env->GetLongField(obj, g_fid_PhotoOptions_hasLocation);
    result.outputFormat = env->GetLongField(obj, g_fid_PhotoOptions_outputFormat);
    return result;
}
static jobject unpack_PhotoOptions_to_jni(JNIEnv* env, const PhotoOptions* st) {
    jobject result = env->NewObject(g_cls_PhotoOptions, g_ctor_PhotoOptions, (jlong)st->flash, (jlong)st->qualityPrioritization, (jlong)st->enableShutterSound, (jlong)st->skipMetadata, (jlong)st->enableAutoRedEyeReduction, (jdouble)st->latitude, (jdouble)st->longitude, (jdouble)st->altitude, (jlong)st->hasLocation, (jlong)st->outputFormat);
    return result;
}
static RecordingOptions pack_RecordingOptions_from_jni(JNIEnv* env, jobject obj) {
    RecordingOptions result;
    result.codec = env->GetLongField(obj, g_fid_RecordingOptions_codec);
    result.fileType = env->GetLongField(obj, g_fid_RecordingOptions_fileType);
    result.bitRate = env->GetLongField(obj, g_fid_RecordingOptions_bitRate);
    result.maxDurationMs = env->GetLongField(obj, g_fid_RecordingOptions_maxDurationMs);
    result.maxFileSizeBytes = env->GetLongField(obj, g_fid_RecordingOptions_maxFileSizeBytes);
    result.latitude = env->GetDoubleField(obj, g_fid_RecordingOptions_latitude);
    result.longitude = env->GetDoubleField(obj, g_fid_RecordingOptions_longitude);
    result.altitude = env->GetDoubleField(obj, g_fid_RecordingOptions_altitude);
    result.hasLocation = env->GetLongField(obj, g_fid_RecordingOptions_hasLocation);
    return result;
}
static jobject unpack_RecordingOptions_to_jni(JNIEnv* env, const RecordingOptions* st) {
    jobject result = env->NewObject(g_cls_RecordingOptions, g_ctor_RecordingOptions, (jlong)st->codec, (jlong)st->fileType, (jlong)st->bitRate, (jlong)st->maxDurationMs, (jlong)st->maxFileSizeBytes, (jdouble)st->latitude, (jdouble)st->longitude, (jdouble)st->altitude, (jlong)st->hasLocation);
    return result;
}

extern "C" {

JNIEXPORT jint JNICALL JNI_OnLoad(JavaVM* vm, void* reserved) {
    g_jvm = vm;
    __android_log_print(ANDROID_LOG_INFO, "Nitrogen", "JNI_OnLoad called for nitro_camera");
    JNIEnv* env = nullptr;
    if (vm->GetEnv(reinterpret_cast<void**>(&env), JNI_VERSION_1_6) != JNI_OK) {
        return -1;
    }
    // Cache standard-library method IDs only — system class loader is always
    // available here. Application class IDs are deferred to initialize().
    {
        jclass cls_class = env->FindClass("java/lang/Class");
        if (cls_class) { g_exc_getName = env->GetMethodID(cls_class, "getName", "()Ljava/lang/String;"); env->DeleteLocalRef(cls_class); }
        jclass throwable_class = env->FindClass("java/lang/Throwable");
        if (throwable_class) { g_exc_getMessage = env->GetMethodID(throwable_class, "getMessage", "()Ljava/lang/String;"); env->DeleteLocalRef(throwable_class); }
    }
    return JNI_VERSION_1_6;
}

static JNIEnv* GetEnv() {
    if (g_jvm == nullptr) { return nullptr; }
    JNIEnv* env = nullptr;
    int status = g_jvm->GetEnv(reinterpret_cast<void**>(&env), JNI_VERSION_1_6);
    if (status == JNI_EDETACHED) {
        g_jvm->AttachCurrentThread(&env, nullptr);
        g_thread_guard.attached = true; // will DetachCurrentThread on thread exit
    }
    return env;
}

// Erases the zero-copy pin for [ptr] (inserted by the stream emit path) and
// deletes the JNI global ref that kept the JVM object — and the direct
// ByteBuffers backing the borrowed @zeroCopy fields — alive while Dart read
// them. Called from the struct release function when the Dart NativeFinalizer
// fires; GetEnv() attaches that thread to the JVM on demand. Without this,
// every delivered zero-copy stream item leaked one global reference and ART
// aborts once its global-reference table (51200 slots) overflows.
void nitro_camera_zero_copy_release(void* ptr) {
    jobject ref = nullptr;
    {
        std::lock_guard<std::mutex> _lk(g_zero_copy_refs_mtx);
        auto it = g_zero_copy_refs.find(ptr);
        if (it != g_zero_copy_refs.end()) {
            ref = it->second;
            g_zero_copy_refs.erase(it);
        }
    }
    if (ref != nullptr) {
        JNIEnv* env = GetEnv();
        if (env != nullptr) { env->DeleteGlobalRef(ref); }
    }
}

NITRO_EXPORT int64_t nitro_camera_create_instance(const char* key) {
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return -1; }
    jmethodID mid = g_mid_create_instance_call;
    if (mid == nullptr) { LOGE("create_instance_call JNI method not cached"); return -1; }
    jstring j_key = env->NewStringUTF(key ? key : "default");
    jlong id = env->CallStaticLongMethod(g_bridgeClass, mid, j_key);
    env->DeleteLocalRef(j_key);
    if (env->ExceptionCheck()) { env->ExceptionClear(); LOGE("create_instance_call threw"); return -1; }
    return (int64_t)id;
}

NITRO_EXPORT void nitro_camera_destroy_instance(int64_t instanceId) {
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID mid = g_mid_destroy_instance_call;
    if (mid == nullptr) { LOGE("destroy_instance_call JNI method not cached"); return; }
    env->CallStaticVoidMethod(g_bridgeClass, mid, (jlong)instanceId);
    if (env->ExceptionCheck()) { env->ExceptionClear(); }
}

void nitro_camera_request_camera_permission(int64_t instanceId, NitroError* _nitro_err, int64_t dart_port) {
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_requestCameraPermission_call;
    if (methodId == nullptr) { LOGE("Method not found: requestCameraPermission_call sig=(JJJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, (jlong)(uintptr_t)_nitro_err, (jlong)dart_port);
    if (env->ExceptionCheck()) {
        nitro_report_jni_exception(env, env->ExceptionOccurred(), nullptr);
    }
    env->PopLocalFrame(nullptr);
}

int64_t nitro_camera_get_camera_permission_status(int64_t instanceId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return 0; }
    jmethodID methodId = g_mid_getCameraPermissionStatus_call;
    if (methodId == nullptr) { LOGE("Method not found: getCameraPermissionStatus_call sig=(J)J"); return 0; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return 0; }
    int64_t res = env->CallStaticLongMethod(g_bridgeClass, methodId, (jlong)instanceId);
    if (env->ExceptionCheck()) {
        nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err);
        env->PopLocalFrame(nullptr);
        return 0;
    }
    env->PopLocalFrame(nullptr);
    return res;
}

void nitro_camera_request_microphone_permission(int64_t instanceId, NitroError* _nitro_err, int64_t dart_port) {
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_requestMicrophonePermission_call;
    if (methodId == nullptr) { LOGE("Method not found: requestMicrophonePermission_call sig=(JJJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, (jlong)(uintptr_t)_nitro_err, (jlong)dart_port);
    if (env->ExceptionCheck()) {
        nitro_report_jni_exception(env, env->ExceptionOccurred(), nullptr);
    }
    env->PopLocalFrame(nullptr);
}

int64_t nitro_camera_get_microphone_permission_status(int64_t instanceId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return 0; }
    jmethodID methodId = g_mid_getMicrophonePermissionStatus_call;
    if (methodId == nullptr) { LOGE("Method not found: getMicrophonePermissionStatus_call sig=(J)J"); return 0; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return 0; }
    int64_t res = env->CallStaticLongMethod(g_bridgeClass, methodId, (jlong)instanceId);
    if (env->ExceptionCheck()) {
        nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err);
        env->PopLocalFrame(nullptr);
        return 0;
    }
    env->PopLocalFrame(nullptr);
    return res;
}

void nitro_camera_get_available_camera_devices_json(int64_t instanceId, NitroError* _nitro_err, int64_t dart_port) {
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_getAvailableCameraDevicesJson_call;
    if (methodId == nullptr) { LOGE("Method not found: getAvailableCameraDevicesJson_call sig=(JJJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, (jlong)(uintptr_t)_nitro_err, (jlong)dart_port);
    if (env->ExceptionCheck()) {
        nitro_report_jni_exception(env, env->ExceptionOccurred(), nullptr);
    }
    env->PopLocalFrame(nullptr);
}

void* nitro_camera_get_available_camera_devices(int64_t instanceId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return nullptr; }
    jmethodID methodId = g_mid_getAvailableCameraDevices_call;
    if (methodId == nullptr) { LOGE("Method not found: getAvailableCameraDevices_call sig=(J)[B"); return nullptr; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return nullptr; }
    jbyteArray jarr = (jbyteArray)env->CallStaticObjectMethod(g_bridgeClass, methodId, (jlong)instanceId);
    if (env->ExceptionCheck()) {
        nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err);
        env->PopLocalFrame(nullptr);
        return nullptr;
    }
    if (jarr == nullptr) {
        env->PopLocalFrame(nullptr);
        return nullptr;
    }
    jsize len = env->GetArrayLength(jarr);
    uint8_t* result = (uint8_t*)malloc(len);
    env->GetByteArrayRegion(jarr, 0, len, (jbyte*)result);
    env->PopLocalFrame(nullptr);
    return result;
}

int64_t nitro_camera_get_device_count(int64_t instanceId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return 0; }
    jmethodID methodId = g_mid_getDeviceCount_call;
    if (methodId == nullptr) { LOGE("Method not found: getDeviceCount_call sig=(J)J"); return 0; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return 0; }
    int64_t res = env->CallStaticLongMethod(g_bridgeClass, methodId, (jlong)instanceId);
    if (env->ExceptionCheck()) {
        nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err);
        env->PopLocalFrame(nullptr);
        return 0;
    }
    env->PopLocalFrame(nullptr);
    return res;
}

void* nitro_camera_get_device(int64_t instanceId, int64_t index, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return nullptr; }
    jmethodID methodId = g_mid_getDevice_call;
    if (methodId == nullptr) { LOGE("Method not found: getDevice_call sig=(JJ)[B"); return nullptr; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return nullptr; }
    jbyteArray jarr = (jbyteArray)env->CallStaticObjectMethod(g_bridgeClass, methodId, (jlong)instanceId, index);
    if (env->ExceptionCheck()) {
        nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err);
        env->PopLocalFrame(nullptr);
        return nullptr;
    }
    if (jarr == nullptr) {
        env->PopLocalFrame(nullptr);
        return nullptr;
    }
    jsize len = env->GetArrayLength(jarr);
    uint8_t* result = (uint8_t*)malloc(len);
    env->GetByteArrayRegion(jarr, 0, len, (jbyte*)result);
    env->PopLocalFrame(nullptr);
    return result;
}

void nitro_camera_open_camera(int64_t instanceId, const char* deviceId, int64_t width, int64_t height, int64_t fps, int64_t enableAudio, NitroError* _nitro_err, int64_t dart_port) {
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_openCamera_call;
    if (methodId == nullptr) { LOGE("Method not found: openCamera_call sig=(JLjava/lang/String;JJJJJJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    jstring j_deviceId = env->NewStringUTF(deviceId);
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, j_deviceId, width, height, fps, enableAudio, (jlong)(uintptr_t)_nitro_err, (jlong)dart_port);
    if (env->ExceptionCheck()) {
        nitro_report_jni_exception(env, env->ExceptionOccurred(), nullptr);
    }
    env->PopLocalFrame(nullptr);
}

void nitro_camera_close_camera(int64_t instanceId, int64_t textureId, NitroError* _nitro_err, int64_t dart_port) {
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_closeCamera_call;
    if (methodId == nullptr) { LOGE("Method not found: closeCamera_call sig=(JJJJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, (jlong)(uintptr_t)_nitro_err, (jlong)dart_port);
    if (env->ExceptionCheck()) {
        nitro_report_jni_exception(env, env->ExceptionOccurred(), nullptr);
    }
    env->PopLocalFrame(nullptr);
}

void nitro_camera_start_preview(int64_t instanceId, int64_t textureId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_startPreview_call;
    if (methodId == nullptr) { LOGE("Method not found: startPreview_call sig=(JJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_stop_preview(int64_t instanceId, int64_t textureId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_stopPreview_call;
    if (methodId == nullptr) { LOGE("Method not found: stopPreview_call sig=(JJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_set_zoom(int64_t instanceId, int64_t textureId, double zoom, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_setZoom_call;
    if (methodId == nullptr) { LOGE("Method not found: setZoom_call sig=(JJD)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, zoom);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_set_focus_point(int64_t instanceId, int64_t textureId, double x, double y, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_setFocusPoint_call;
    if (methodId == nullptr) { LOGE("Method not found: setFocusPoint_call sig=(JJDD)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, x, y);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_set_auto_focus(int64_t instanceId, int64_t textureId, int64_t mode, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_setAutoFocus_call;
    if (methodId == nullptr) { LOGE("Method not found: setAutoFocus_call sig=(JJJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, mode);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_set_exposure(int64_t instanceId, int64_t textureId, double value, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_setExposure_call;
    if (methodId == nullptr) { LOGE("Method not found: setExposure_call sig=(JJD)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, value);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_set_flash(int64_t instanceId, int64_t textureId, int64_t mode, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_setFlash_call;
    if (methodId == nullptr) { LOGE("Method not found: setFlash_call sig=(JJJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, mode);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_set_torch(int64_t instanceId, int64_t textureId, int64_t enabled, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_setTorch_call;
    if (methodId == nullptr) { LOGE("Method not found: setTorch_call sig=(JJJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, enabled);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_set_white_balance(int64_t instanceId, int64_t textureId, int64_t temperature, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_setWhiteBalance_call;
    if (methodId == nullptr) { LOGE("Method not found: setWhiteBalance_call sig=(JJJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, temperature);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_set_hdr(int64_t instanceId, int64_t textureId, int64_t enabled, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_setHdr_call;
    if (methodId == nullptr) { LOGE("Method not found: setHdr_call sig=(JJJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, enabled);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_take_photo(int64_t instanceId, int64_t textureId, NitroError* _nitro_err, int64_t dart_port) {
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_takePhoto_call;
    if (methodId == nullptr) { LOGE("Method not found: takePhoto_call sig=(JJJJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, (jlong)(uintptr_t)_nitro_err, (jlong)dart_port);
    if (env->ExceptionCheck()) {
        nitro_report_jni_exception(env, env->ExceptionOccurred(), nullptr);
    }
    env->PopLocalFrame(nullptr);
}

void nitro_camera_start_video_recording(int64_t instanceId, int64_t textureId, const char* outputPath, void* options, NitroError* _nitro_err, int64_t dart_port) {
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_startVideoRecording_call;
    if (methodId == nullptr) { LOGE("Method not found: startVideoRecording_call sig=(JJLjava/lang/String;Lnitro/nitro_camera_module/RecordingOptions;JJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    jstring j_outputPath = env->NewStringUTF(outputPath);
    jobject jobj_options = unpack_RecordingOptions_to_jni(env, (const RecordingOptions*)options);
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, j_outputPath, jobj_options, (jlong)(uintptr_t)_nitro_err, (jlong)dart_port);
    if (env->ExceptionCheck()) {
        nitro_report_jni_exception(env, env->ExceptionOccurred(), nullptr);
    }
    env->PopLocalFrame(nullptr);
}

void nitro_camera_stop_video_recording(int64_t instanceId, int64_t textureId, NitroError* _nitro_err, int64_t dart_port) {
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_stopVideoRecording_call;
    if (methodId == nullptr) { LOGE("Method not found: stopVideoRecording_call sig=(JJJJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, (jlong)(uintptr_t)_nitro_err, (jlong)dart_port);
    if (env->ExceptionCheck()) {
        nitro_report_jni_exception(env, env->ExceptionOccurred(), nullptr);
    }
    env->PopLocalFrame(nullptr);
}

void nitro_camera_pause_recording(int64_t instanceId, int64_t textureId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_pauseRecording_call;
    if (methodId == nullptr) { LOGE("Method not found: pauseRecording_call sig=(JJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_resume_recording(int64_t instanceId, int64_t textureId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_resumeRecording_call;
    if (methodId == nullptr) { LOGE("Method not found: resumeRecording_call sig=(JJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_cancel_recording(int64_t instanceId, int64_t textureId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_cancelRecording_call;
    if (methodId == nullptr) { LOGE("Method not found: cancelRecording_call sig=(JJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_enable_frame_processing(int64_t instanceId, int64_t textureId, int64_t enabled, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_enableFrameProcessing_call;
    if (methodId == nullptr) { LOGE("Method not found: enableFrameProcessing_call sig=(JJJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, enabled);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_set_frame_format(int64_t instanceId, int64_t textureId, int64_t format, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_setFrameFormat_call;
    if (methodId == nullptr) { LOGE("Method not found: setFrameFormat_call sig=(JJJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, format);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_set_sampling_rate(int64_t instanceId, int64_t textureId, int64_t samplingRate, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_setSamplingRate_call;
    if (methodId == nullptr) { LOGE("Method not found: setSamplingRate_call sig=(JJJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, samplingRate);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_set_filter_shader(int64_t instanceId, int64_t textureId, const char* shaderSource, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_setFilterShader_call;
    if (methodId == nullptr) { LOGE("Method not found: setFilterShader_call sig=(JJLjava/lang/String;)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    jstring j_shaderSource = env->NewStringUTF(shaderSource);
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, j_shaderSource);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_update_overlay(int64_t instanceId, int64_t textureId, uint8_t* overlayData, size_t overlayData_length, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_updateOverlay_call;
    if (methodId == nullptr) { LOGE("Method not found: updateOverlay_call sig=(JJLjava/nio/ByteBuffer;)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    if (overlayData_length < 0) {
        nitro_report_error("ArgumentError", "overlayData: TypedData length cannot be negative", nullptr, nullptr);
        env->PopLocalFrame(nullptr);
        return;
    }
    if (overlayData == nullptr) {
        nitro_report_error("ArgumentError", "overlayData: TypedData pointer is null for non-empty buffer", nullptr, nullptr);
        env->PopLocalFrame(nullptr);
        return;
    }
    if (overlayData_length > INT64_MAX / (int64_t)sizeof(uint8_t)) {
        nitro_report_error("ArgumentError", "overlayData: TypedData byte length overflow", nullptr, nullptr);
        env->PopLocalFrame(nullptr);
        return;
    }
    int64_t overlayData_byte_length = overlayData_length * (int64_t)sizeof(uint8_t);
    jobject j_overlayData = env->NewDirectByteBuffer(overlayData, overlayData_byte_length);
    if (j_overlayData == nullptr) {
        nitro_report_error("ArgumentError", "overlayData: failed to create direct ByteBuffer", nullptr, nullptr);
        env->PopLocalFrame(nullptr);
        return;
    }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, j_overlayData);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_configure(int64_t instanceId, int64_t textureId, void* config, NitroError* _nitro_err, int64_t dart_port) {
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_configure_call;
    if (methodId == nullptr) { LOGE("Method not found: configure_call sig=(JJLnitro/nitro_camera_module/CameraConfig;JJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    jobject jobj_config = unpack_CameraConfig_to_jni(env, (const CameraConfig*)config);
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, jobj_config, (jlong)(uintptr_t)_nitro_err, (jlong)dart_port);
    if (env->ExceptionCheck()) {
        nitro_report_jni_exception(env, env->ExceptionOccurred(), nullptr);
    }
    env->PopLocalFrame(nullptr);
}

const char* nitro_camera_get_session_state_json(int64_t instanceId, int64_t textureId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return nullptr; }
    jmethodID methodId = g_mid_getSessionStateJson_call;
    if (methodId == nullptr) { LOGE("Method not found: getSessionStateJson_call sig=(JJ)Ljava/lang/String;"); return nullptr; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return nullptr; }
    jstring jstr = (jstring)env->CallStaticObjectMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId);
    if (env->ExceptionCheck()) {
        nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err);
        env->PopLocalFrame(nullptr);
        return nullptr;
    }
    if (jstr == nullptr) {
        env->PopLocalFrame(nullptr);
        return nullptr;
    }
    const char* nativeStr = env->GetStringUTFChars(jstr, 0);
    char* result = strdup(nativeStr);
    env->ReleaseStringUTFChars(jstr, nativeStr);
    env->PopLocalFrame(nullptr);
    return result;
}

void nitro_camera_set_video_stabilization(int64_t instanceId, int64_t textureId, int64_t mode, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_setVideoStabilization_call;
    if (methodId == nullptr) { LOGE("Method not found: setVideoStabilization_call sig=(JJJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, mode);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_set_low_light_boost(int64_t instanceId, int64_t textureId, int64_t enabled, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_setLowLightBoost_call;
    if (methodId == nullptr) { LOGE("Method not found: setLowLightBoost_call sig=(JJJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, enabled);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_set_torch_level(int64_t instanceId, int64_t textureId, double level, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_setTorchLevel_call;
    if (methodId == nullptr) { LOGE("Method not found: setTorchLevel_call sig=(JJD)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, level);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_lock_exposure(int64_t instanceId, int64_t textureId, int64_t locked, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_lockExposure_call;
    if (methodId == nullptr) { LOGE("Method not found: lockExposure_call sig=(JJJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, locked);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_lock_focus(int64_t instanceId, int64_t textureId, int64_t locked, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_lockFocus_call;
    if (methodId == nullptr) { LOGE("Method not found: lockFocus_call sig=(JJJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, locked);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_lock_white_balance(int64_t instanceId, int64_t textureId, int64_t locked, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_lockWhiteBalance_call;
    if (methodId == nullptr) { LOGE("Method not found: lockWhiteBalance_call sig=(JJJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, locked);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_set_target_orientation(int64_t instanceId, int64_t textureId, int64_t degrees, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_setTargetOrientation_call;
    if (methodId == nullptr) { LOGE("Method not found: setTargetOrientation_call sig=(JJJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, degrees);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_set_distortion_correction(int64_t instanceId, int64_t textureId, int64_t enabled, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_setDistortionCorrection_call;
    if (methodId == nullptr) { LOGE("Method not found: setDistortionCorrection_call sig=(JJJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, enabled);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_enable_orientation_events(int64_t instanceId, int64_t enabled, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_enableOrientationEvents_call;
    if (methodId == nullptr) { LOGE("Method not found: enableOrientationEvents_call sig=(JJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, enabled);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_enable_device_availability_events(int64_t instanceId, int64_t enabled, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_enableDeviceAvailabilityEvents_call;
    if (methodId == nullptr) { LOGE("Method not found: enableDeviceAvailabilityEvents_call sig=(JJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, enabled);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_set_native_detector(int64_t instanceId, int64_t textureId, const char* detector, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_setNativeDetector_call;
    if (methodId == nullptr) { LOGE("Method not found: setNativeDetector_call sig=(JJLjava/lang/String;)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    jstring j_detector = env->NewStringUTF(detector);
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, j_detector);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

const char* nitro_camera_get_concurrent_camera_ids_json(int64_t instanceId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return nullptr; }
    jmethodID methodId = g_mid_getConcurrentCameraIdsJson_call;
    if (methodId == nullptr) { LOGE("Method not found: getConcurrentCameraIdsJson_call sig=(J)Ljava/lang/String;"); return nullptr; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return nullptr; }
    jstring jstr = (jstring)env->CallStaticObjectMethod(g_bridgeClass, methodId, (jlong)instanceId);
    if (env->ExceptionCheck()) {
        nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err);
        env->PopLocalFrame(nullptr);
        return nullptr;
    }
    if (jstr == nullptr) {
        env->PopLocalFrame(nullptr);
        return nullptr;
    }
    const char* nativeStr = env->GetStringUTFChars(jstr, 0);
    char* result = strdup(nativeStr);
    env->ReleaseStringUTFChars(jstr, nativeStr);
    env->PopLocalFrame(nullptr);
    return result;
}

void nitro_camera_take_photo_with_options(int64_t instanceId, int64_t textureId, void* options, NitroError* _nitro_err, int64_t dart_port) {
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_takePhotoWithOptions_call;
    if (methodId == nullptr) { LOGE("Method not found: takePhotoWithOptions_call sig=(JJLnitro/nitro_camera_module/PhotoOptions;JJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    jobject jobj_options = unpack_PhotoOptions_to_jni(env, (const PhotoOptions*)options);
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, jobj_options, (jlong)(uintptr_t)_nitro_err, (jlong)dart_port);
    if (env->ExceptionCheck()) {
        nitro_report_jni_exception(env, env->ExceptionOccurred(), nullptr);
    }
    env->PopLocalFrame(nullptr);
}

void nitro_camera_take_snapshot(int64_t instanceId, int64_t textureId, NitroError* _nitro_err, int64_t dart_port) {
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_takeSnapshot_call;
    if (methodId == nullptr) { LOGE("Method not found: takeSnapshot_call sig=(JJJJ)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, textureId, (jlong)(uintptr_t)_nitro_err, (jlong)dart_port);
    if (env->ExceptionCheck()) {
        nitro_report_jni_exception(env, env->ExceptionOccurred(), nullptr);
    }
    env->PopLocalFrame(nullptr);
}

void nitro_camera_reset(int64_t instanceId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }  // S8: clear slot
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_reset_call;
    if (methodId == nullptr) { LOGE("Method not found: reset_call sig=(J)V"); return; }

    nitro_camera_clear_error();
    if (env->PushLocalFrame(16) != 0) { return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId);
    env->PopLocalFrame(nullptr);
    if (env->ExceptionCheck()) { nitro_report_jni_exception(env, env->ExceptionOccurred(), _nitro_err); }
}

void nitro_camera_register_frame_stream_stream(int64_t instanceId, int64_t dart_port) {
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_nitro_camera_register_frame_stream_stream_call;
    if (methodId == nullptr) { LOGE("Method not found: nitro_camera_register_frame_stream_stream_call sig=(JJ)V"); return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, dart_port);
}

void nitro_camera_release_frame_stream_stream(int64_t dart_port) {
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_nitro_camera_release_frame_stream_stream_call;
    if (methodId == nullptr) { LOGE("Method not found: nitro_camera_release_frame_stream_stream_call sig=(J)V"); return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, dart_port);
}

JNIEXPORT jboolean JNICALL Java_nitro_nitro_1camera_1module_NitroCameraJniBridge_emit_1frameStream(JNIEnv* env, jobject thiz, jlong dartPort, jobject item) {
    Dart_CObject obj;
    CameraFrame* st_ptr = (CameraFrame*)malloc(sizeof(CameraFrame));
    *st_ptr = pack_CameraFrame_from_jni(env, item);
    // Check if pack_ threw an exception (e.g., heap ByteBuffer for @ZeroCopy)
    if (env->ExceptionCheck()) {
        env->ExceptionDescribe();
        env->ExceptionClear();
        free(st_ptr);
        return JNI_FALSE;
    }
    obj.type = Dart_CObject_kInt64;
    obj.value.as_int64 = (intptr_t)st_ptr;
    // Keep item alive so JVM does not GC the ByteBuffer backing st_ptr's zero-copy fields.
    {
        std::lock_guard<std::mutex> _lk(g_zero_copy_refs_mtx);
        g_zero_copy_refs[(void*)st_ptr] = env->NewGlobalRef(item);
    }
    if (!Dart_PostCObject_DL(dartPort, &obj)) {
        {
            std::lock_guard<std::mutex> _lk(g_zero_copy_refs_mtx);
            auto _it = g_zero_copy_refs.find((void*)st_ptr);
            if (_it != g_zero_copy_refs.end()) {
                env->DeleteGlobalRef(_it->second);
                g_zero_copy_refs.erase(_it);
            }
        }
        free(st_ptr);
        return JNI_FALSE;
    }
    return JNI_TRUE;
}

void nitro_camera_register_event_stream_stream(int64_t instanceId, int64_t dart_port) {
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_nitro_camera_register_event_stream_stream_call;
    if (methodId == nullptr) { LOGE("Method not found: nitro_camera_register_event_stream_stream_call sig=(JJ)V"); return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, (jlong)instanceId, dart_port);
}

void nitro_camera_release_event_stream_stream(int64_t dart_port) {
    JNIEnv* env = GetEnv();
    if (env == nullptr) { return; }
    jmethodID methodId = g_mid_nitro_camera_release_event_stream_stream_call;
    if (methodId == nullptr) { LOGE("Method not found: nitro_camera_release_event_stream_stream_call sig=(J)V"); return; }
    env->CallStaticVoidMethod(g_bridgeClass, methodId, dart_port);
}

JNIEXPORT jboolean JNICALL Java_nitro_nitro_1camera_1module_NitroCameraJniBridge_emit_1eventStream(JNIEnv* env, jobject thiz, jlong dartPort, jbyteArray item) {
    Dart_CObject obj;
    jsize len = env->GetArrayLength(item);
    uint8_t* buf = (uint8_t*)malloc((size_t)len);
    env->GetByteArrayRegion(item, 0, len, (jbyte*)buf);
    obj.type = Dart_CObject_kInt64;
    obj.value.as_int64 = (intptr_t)buf;
    if (!Dart_PostCObject_DL(dartPort, &obj)) {
        free(buf);
        return JNI_FALSE;
    }
    return JNI_TRUE;
}

JNIEXPORT void JNICALL Java_nitro_nitro_1camera_1module_NitroCameraJniBridge_initialize(JNIEnv* env, jobject thiz, jclass localClass) {
    if (g_bridgeClass == nullptr) {
        g_bridgeClass = (jclass)env->NewGlobalRef(localClass);
        env->DeleteLocalRef(localClass);
    }
    // Re-cache method IDs every time (safe; idempotent; works even if JNI_OnLoad
    // could not find the app class. initialize() is called from Kotlin with the
    // correct class loader.)
    if (g_bridgeClass != nullptr) {
        // Cache bridge method IDs
        // Each GetStaticMethodID is followed by an ExceptionClear() guard:
        // if ANY lookup fails it throws NoSuchMethodError, and calling the
        // NEXT GetStaticMethodID with a pending exception aborts the JVM on
        // Android >= API 26 (strict JNI mode). Clearing after each failure
        // lets the remaining lookups proceed and logs the missing method.
        g_mid_create_instance_call = env->GetStaticMethodID(g_bridgeClass, "create_instance_call", "(Ljava/lang/String;)J");
        if (!g_mid_create_instance_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: create_instance_call sig=(Ljava/lang/String;)J"); }
        g_mid_destroy_instance_call = env->GetStaticMethodID(g_bridgeClass, "destroy_instance_call", "(J)V");
        if (!g_mid_destroy_instance_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: destroy_instance_call sig=(J)V"); }
        g_mid_requestCameraPermission_call = env->GetStaticMethodID(g_bridgeClass, "requestCameraPermission_call", "(JJJ)V");
        if (!g_mid_requestCameraPermission_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: requestCameraPermission_call sig=(JJJ)V"); }
        g_mid_getCameraPermissionStatus_call = env->GetStaticMethodID(g_bridgeClass, "getCameraPermissionStatus_call", "(J)J");
        if (!g_mid_getCameraPermissionStatus_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: getCameraPermissionStatus_call sig=(J)J"); }
        g_mid_requestMicrophonePermission_call = env->GetStaticMethodID(g_bridgeClass, "requestMicrophonePermission_call", "(JJJ)V");
        if (!g_mid_requestMicrophonePermission_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: requestMicrophonePermission_call sig=(JJJ)V"); }
        g_mid_getMicrophonePermissionStatus_call = env->GetStaticMethodID(g_bridgeClass, "getMicrophonePermissionStatus_call", "(J)J");
        if (!g_mid_getMicrophonePermissionStatus_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: getMicrophonePermissionStatus_call sig=(J)J"); }
        g_mid_getAvailableCameraDevicesJson_call = env->GetStaticMethodID(g_bridgeClass, "getAvailableCameraDevicesJson_call", "(JJJ)V");
        if (!g_mid_getAvailableCameraDevicesJson_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: getAvailableCameraDevicesJson_call sig=(JJJ)V"); }
        g_mid_getAvailableCameraDevices_call = env->GetStaticMethodID(g_bridgeClass, "getAvailableCameraDevices_call", "(J)[B");
        if (!g_mid_getAvailableCameraDevices_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: getAvailableCameraDevices_call sig=(J)[B"); }
        g_mid_getDeviceCount_call = env->GetStaticMethodID(g_bridgeClass, "getDeviceCount_call", "(J)J");
        if (!g_mid_getDeviceCount_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: getDeviceCount_call sig=(J)J"); }
        g_mid_getDevice_call = env->GetStaticMethodID(g_bridgeClass, "getDevice_call", "(JJ)[B");
        if (!g_mid_getDevice_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: getDevice_call sig=(JJ)[B"); }
        g_mid_openCamera_call = env->GetStaticMethodID(g_bridgeClass, "openCamera_call", "(JLjava/lang/String;JJJJJJ)V");
        if (!g_mid_openCamera_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: openCamera_call sig=(JLjava/lang/String;JJJJJJ)V"); }
        g_mid_closeCamera_call = env->GetStaticMethodID(g_bridgeClass, "closeCamera_call", "(JJJJ)V");
        if (!g_mid_closeCamera_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: closeCamera_call sig=(JJJJ)V"); }
        g_mid_startPreview_call = env->GetStaticMethodID(g_bridgeClass, "startPreview_call", "(JJ)V");
        if (!g_mid_startPreview_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: startPreview_call sig=(JJ)V"); }
        g_mid_stopPreview_call = env->GetStaticMethodID(g_bridgeClass, "stopPreview_call", "(JJ)V");
        if (!g_mid_stopPreview_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: stopPreview_call sig=(JJ)V"); }
        g_mid_setZoom_call = env->GetStaticMethodID(g_bridgeClass, "setZoom_call", "(JJD)V");
        if (!g_mid_setZoom_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: setZoom_call sig=(JJD)V"); }
        g_mid_setFocusPoint_call = env->GetStaticMethodID(g_bridgeClass, "setFocusPoint_call", "(JJDD)V");
        if (!g_mid_setFocusPoint_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: setFocusPoint_call sig=(JJDD)V"); }
        g_mid_setAutoFocus_call = env->GetStaticMethodID(g_bridgeClass, "setAutoFocus_call", "(JJJ)V");
        if (!g_mid_setAutoFocus_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: setAutoFocus_call sig=(JJJ)V"); }
        g_mid_setExposure_call = env->GetStaticMethodID(g_bridgeClass, "setExposure_call", "(JJD)V");
        if (!g_mid_setExposure_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: setExposure_call sig=(JJD)V"); }
        g_mid_setFlash_call = env->GetStaticMethodID(g_bridgeClass, "setFlash_call", "(JJJ)V");
        if (!g_mid_setFlash_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: setFlash_call sig=(JJJ)V"); }
        g_mid_setTorch_call = env->GetStaticMethodID(g_bridgeClass, "setTorch_call", "(JJJ)V");
        if (!g_mid_setTorch_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: setTorch_call sig=(JJJ)V"); }
        g_mid_setWhiteBalance_call = env->GetStaticMethodID(g_bridgeClass, "setWhiteBalance_call", "(JJJ)V");
        if (!g_mid_setWhiteBalance_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: setWhiteBalance_call sig=(JJJ)V"); }
        g_mid_setHdr_call = env->GetStaticMethodID(g_bridgeClass, "setHdr_call", "(JJJ)V");
        if (!g_mid_setHdr_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: setHdr_call sig=(JJJ)V"); }
        g_mid_takePhoto_call = env->GetStaticMethodID(g_bridgeClass, "takePhoto_call", "(JJJJ)V");
        if (!g_mid_takePhoto_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: takePhoto_call sig=(JJJJ)V"); }
        g_mid_startVideoRecording_call = env->GetStaticMethodID(g_bridgeClass, "startVideoRecording_call", "(JJLjava/lang/String;Lnitro/nitro_camera_module/RecordingOptions;JJ)V");
        if (!g_mid_startVideoRecording_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: startVideoRecording_call sig=(JJLjava/lang/String;Lnitro/nitro_camera_module/RecordingOptions;JJ)V"); }
        g_mid_stopVideoRecording_call = env->GetStaticMethodID(g_bridgeClass, "stopVideoRecording_call", "(JJJJ)V");
        if (!g_mid_stopVideoRecording_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: stopVideoRecording_call sig=(JJJJ)V"); }
        g_mid_pauseRecording_call = env->GetStaticMethodID(g_bridgeClass, "pauseRecording_call", "(JJ)V");
        if (!g_mid_pauseRecording_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: pauseRecording_call sig=(JJ)V"); }
        g_mid_resumeRecording_call = env->GetStaticMethodID(g_bridgeClass, "resumeRecording_call", "(JJ)V");
        if (!g_mid_resumeRecording_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: resumeRecording_call sig=(JJ)V"); }
        g_mid_cancelRecording_call = env->GetStaticMethodID(g_bridgeClass, "cancelRecording_call", "(JJ)V");
        if (!g_mid_cancelRecording_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: cancelRecording_call sig=(JJ)V"); }
        g_mid_enableFrameProcessing_call = env->GetStaticMethodID(g_bridgeClass, "enableFrameProcessing_call", "(JJJ)V");
        if (!g_mid_enableFrameProcessing_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: enableFrameProcessing_call sig=(JJJ)V"); }
        g_mid_setFrameFormat_call = env->GetStaticMethodID(g_bridgeClass, "setFrameFormat_call", "(JJJ)V");
        if (!g_mid_setFrameFormat_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: setFrameFormat_call sig=(JJJ)V"); }
        g_mid_setSamplingRate_call = env->GetStaticMethodID(g_bridgeClass, "setSamplingRate_call", "(JJJ)V");
        if (!g_mid_setSamplingRate_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: setSamplingRate_call sig=(JJJ)V"); }
        g_mid_setFilterShader_call = env->GetStaticMethodID(g_bridgeClass, "setFilterShader_call", "(JJLjava/lang/String;)V");
        if (!g_mid_setFilterShader_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: setFilterShader_call sig=(JJLjava/lang/String;)V"); }
        g_mid_updateOverlay_call = env->GetStaticMethodID(g_bridgeClass, "updateOverlay_call", "(JJLjava/nio/ByteBuffer;)V");
        if (!g_mid_updateOverlay_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: updateOverlay_call sig=(JJLjava/nio/ByteBuffer;)V"); }
        g_mid_configure_call = env->GetStaticMethodID(g_bridgeClass, "configure_call", "(JJLnitro/nitro_camera_module/CameraConfig;JJ)V");
        if (!g_mid_configure_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: configure_call sig=(JJLnitro/nitro_camera_module/CameraConfig;JJ)V"); }
        g_mid_getSessionStateJson_call = env->GetStaticMethodID(g_bridgeClass, "getSessionStateJson_call", "(JJ)Ljava/lang/String;");
        if (!g_mid_getSessionStateJson_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: getSessionStateJson_call sig=(JJ)Ljava/lang/String;"); }
        g_mid_setVideoStabilization_call = env->GetStaticMethodID(g_bridgeClass, "setVideoStabilization_call", "(JJJ)V");
        if (!g_mid_setVideoStabilization_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: setVideoStabilization_call sig=(JJJ)V"); }
        g_mid_setLowLightBoost_call = env->GetStaticMethodID(g_bridgeClass, "setLowLightBoost_call", "(JJJ)V");
        if (!g_mid_setLowLightBoost_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: setLowLightBoost_call sig=(JJJ)V"); }
        g_mid_setTorchLevel_call = env->GetStaticMethodID(g_bridgeClass, "setTorchLevel_call", "(JJD)V");
        if (!g_mid_setTorchLevel_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: setTorchLevel_call sig=(JJD)V"); }
        g_mid_lockExposure_call = env->GetStaticMethodID(g_bridgeClass, "lockExposure_call", "(JJJ)V");
        if (!g_mid_lockExposure_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: lockExposure_call sig=(JJJ)V"); }
        g_mid_lockFocus_call = env->GetStaticMethodID(g_bridgeClass, "lockFocus_call", "(JJJ)V");
        if (!g_mid_lockFocus_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: lockFocus_call sig=(JJJ)V"); }
        g_mid_lockWhiteBalance_call = env->GetStaticMethodID(g_bridgeClass, "lockWhiteBalance_call", "(JJJ)V");
        if (!g_mid_lockWhiteBalance_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: lockWhiteBalance_call sig=(JJJ)V"); }
        g_mid_setTargetOrientation_call = env->GetStaticMethodID(g_bridgeClass, "setTargetOrientation_call", "(JJJ)V");
        if (!g_mid_setTargetOrientation_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: setTargetOrientation_call sig=(JJJ)V"); }
        g_mid_setDistortionCorrection_call = env->GetStaticMethodID(g_bridgeClass, "setDistortionCorrection_call", "(JJJ)V");
        if (!g_mid_setDistortionCorrection_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: setDistortionCorrection_call sig=(JJJ)V"); }
        g_mid_enableOrientationEvents_call = env->GetStaticMethodID(g_bridgeClass, "enableOrientationEvents_call", "(JJ)V");
        if (!g_mid_enableOrientationEvents_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: enableOrientationEvents_call sig=(JJ)V"); }
        g_mid_enableDeviceAvailabilityEvents_call = env->GetStaticMethodID(g_bridgeClass, "enableDeviceAvailabilityEvents_call", "(JJ)V");
        if (!g_mid_enableDeviceAvailabilityEvents_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: enableDeviceAvailabilityEvents_call sig=(JJ)V"); }
        g_mid_setNativeDetector_call = env->GetStaticMethodID(g_bridgeClass, "setNativeDetector_call", "(JJLjava/lang/String;)V");
        if (!g_mid_setNativeDetector_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: setNativeDetector_call sig=(JJLjava/lang/String;)V"); }
        g_mid_getConcurrentCameraIdsJson_call = env->GetStaticMethodID(g_bridgeClass, "getConcurrentCameraIdsJson_call", "(J)Ljava/lang/String;");
        if (!g_mid_getConcurrentCameraIdsJson_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: getConcurrentCameraIdsJson_call sig=(J)Ljava/lang/String;"); }
        g_mid_takePhotoWithOptions_call = env->GetStaticMethodID(g_bridgeClass, "takePhotoWithOptions_call", "(JJLnitro/nitro_camera_module/PhotoOptions;JJ)V");
        if (!g_mid_takePhotoWithOptions_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: takePhotoWithOptions_call sig=(JJLnitro/nitro_camera_module/PhotoOptions;JJ)V"); }
        g_mid_takeSnapshot_call = env->GetStaticMethodID(g_bridgeClass, "takeSnapshot_call", "(JJJJ)V");
        if (!g_mid_takeSnapshot_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: takeSnapshot_call sig=(JJJJ)V"); }
        g_mid_reset_call = env->GetStaticMethodID(g_bridgeClass, "reset_call", "(J)V");
        if (!g_mid_reset_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: reset_call sig=(J)V"); }
        g_mid_nitro_camera_register_frame_stream_stream_call = env->GetStaticMethodID(g_bridgeClass, "nitro_camera_register_frame_stream_stream_call", "(JJ)V");
        if (!g_mid_nitro_camera_register_frame_stream_stream_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: nitro_camera_register_frame_stream_stream_call sig=(JJ)V"); }
        g_mid_nitro_camera_release_frame_stream_stream_call = env->GetStaticMethodID(g_bridgeClass, "nitro_camera_release_frame_stream_stream_call", "(J)V");
        if (!g_mid_nitro_camera_release_frame_stream_stream_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: nitro_camera_release_frame_stream_stream_call sig=(J)V"); }
        g_mid_nitro_camera_register_event_stream_stream_call = env->GetStaticMethodID(g_bridgeClass, "nitro_camera_register_event_stream_stream_call", "(JJ)V");
        if (!g_mid_nitro_camera_register_event_stream_stream_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: nitro_camera_register_event_stream_stream_call sig=(JJ)V"); }
        g_mid_nitro_camera_release_event_stream_stream_call = env->GetStaticMethodID(g_bridgeClass, "nitro_camera_release_event_stream_stream_call", "(J)V");
        if (!g_mid_nitro_camera_release_event_stream_stream_call && env->ExceptionCheck()) { env->ExceptionClear(); LOGE("Method not found: nitro_camera_release_event_stream_stream_call sig=(J)V"); }
    }

    // Cache CameraEvent class + encode() for stream serialisation
    {
        jclass local_cls_CameraEvent = env->FindClass("nitro/nitro_camera_module/CameraEvent");
        if (local_cls_CameraEvent != nullptr) {
            g_cls_CameraEvent = (jclass)env->NewGlobalRef(local_cls_CameraEvent);
            env->DeleteLocalRef(local_cls_CameraEvent);
            g_mid_CameraEvent_encode = env->GetMethodID(g_cls_CameraEvent, "encode", "()[B");
        } else {
            LOGE("Failed to find class nitro/nitro_camera_module/CameraEvent");
        }
    }
    // Cache struct class + ctor + field IDs
    {
        jclass local_cls_CameraFrame = env->FindClass("nitro/nitro_camera_module/CameraFrame");
        if (local_cls_CameraFrame != nullptr) {
            g_cls_CameraFrame = (jclass)env->NewGlobalRef(local_cls_CameraFrame);
            env->DeleteLocalRef(local_cls_CameraFrame);
            g_ctor_CameraFrame = env->GetMethodID(g_cls_CameraFrame, "<init>", "(Ljava/nio/ByteBuffer;JJJJJJJJJ)V");
            g_fid_CameraFrame_pixels = env->GetFieldID(g_cls_CameraFrame, "pixels", "Ljava/nio/ByteBuffer;");
            g_fid_CameraFrame_size = env->GetFieldID(g_cls_CameraFrame, "size", "J");
            g_fid_CameraFrame_width = env->GetFieldID(g_cls_CameraFrame, "width", "J");
            g_fid_CameraFrame_height = env->GetFieldID(g_cls_CameraFrame, "height", "J");
            g_fid_CameraFrame_timestamp = env->GetFieldID(g_cls_CameraFrame, "timestamp", "J");
            g_fid_CameraFrame_orientation = env->GetFieldID(g_cls_CameraFrame, "orientation", "J");
            g_fid_CameraFrame_textureId = env->GetFieldID(g_cls_CameraFrame, "textureId", "J");
            g_fid_CameraFrame_bytesPerRow = env->GetFieldID(g_cls_CameraFrame, "bytesPerRow", "J");
            g_fid_CameraFrame_pixelFormat = env->GetFieldID(g_cls_CameraFrame, "pixelFormat", "J");
            g_fid_CameraFrame_isMirrored = env->GetFieldID(g_cls_CameraFrame, "isMirrored", "J");
        }
    }
    {
        jclass local_cls_CameraConfig = env->FindClass("nitro/nitro_camera_module/CameraConfig");
        if (local_cls_CameraConfig != nullptr) {
            g_cls_CameraConfig = (jclass)env->NewGlobalRef(local_cls_CameraConfig);
            env->DeleteLocalRef(local_cls_CameraConfig);
            g_ctor_CameraConfig = env->GetMethodID(g_cls_CameraConfig, "<init>", "(DDJJDJJJJJJJJJ)V");
            g_fid_CameraConfig_zoom = env->GetFieldID(g_cls_CameraConfig, "zoom", "D");
            g_fid_CameraConfig_exposure = env->GetFieldID(g_cls_CameraConfig, "exposure", "D");
            g_fid_CameraConfig_flash = env->GetFieldID(g_cls_CameraConfig, "flash", "J");
            g_fid_CameraConfig_torch = env->GetFieldID(g_cls_CameraConfig, "torch", "J");
            g_fid_CameraConfig_torchLevel = env->GetFieldID(g_cls_CameraConfig, "torchLevel", "D");
            g_fid_CameraConfig_whiteBalanceKelvin = env->GetFieldID(g_cls_CameraConfig, "whiteBalanceKelvin", "J");
            g_fid_CameraConfig_videoHdr = env->GetFieldID(g_cls_CameraConfig, "videoHdr", "J");
            g_fid_CameraConfig_lowLightBoost = env->GetFieldID(g_cls_CameraConfig, "lowLightBoost", "J");
            g_fid_CameraConfig_autoFocus = env->GetFieldID(g_cls_CameraConfig, "autoFocus", "J");
            g_fid_CameraConfig_videoStabilization = env->GetFieldID(g_cls_CameraConfig, "videoStabilization", "J");
            g_fid_CameraConfig_active = env->GetFieldID(g_cls_CameraConfig, "active", "J");
            g_fid_CameraConfig_enableFrameProcessing = env->GetFieldID(g_cls_CameraConfig, "enableFrameProcessing", "J");
            g_fid_CameraConfig_pixelFormat = env->GetFieldID(g_cls_CameraConfig, "pixelFormat", "J");
            g_fid_CameraConfig_samplingRate = env->GetFieldID(g_cls_CameraConfig, "samplingRate", "J");
        }
    }
    {
        jclass local_cls_ResolvedConfig = env->FindClass("nitro/nitro_camera_module/ResolvedConfig");
        if (local_cls_ResolvedConfig != nullptr) {
            g_cls_ResolvedConfig = (jclass)env->NewGlobalRef(local_cls_ResolvedConfig);
            env->DeleteLocalRef(local_cls_ResolvedConfig);
            g_ctor_ResolvedConfig = env->GetMethodID(g_cls_ResolvedConfig, "<init>", "(JJJJJJJ)V");
            g_fid_ResolvedConfig_width = env->GetFieldID(g_cls_ResolvedConfig, "width", "J");
            g_fid_ResolvedConfig_height = env->GetFieldID(g_cls_ResolvedConfig, "height", "J");
            g_fid_ResolvedConfig_fps = env->GetFieldID(g_cls_ResolvedConfig, "fps", "J");
            g_fid_ResolvedConfig_pixelFormat = env->GetFieldID(g_cls_ResolvedConfig, "pixelFormat", "J");
            g_fid_ResolvedConfig_videoHdrEnabled = env->GetFieldID(g_cls_ResolvedConfig, "videoHdrEnabled", "J");
            g_fid_ResolvedConfig_autoFocusSystem = env->GetFieldID(g_cls_ResolvedConfig, "autoFocusSystem", "J");
            g_fid_ResolvedConfig_active = env->GetFieldID(g_cls_ResolvedConfig, "active", "J");
        }
    }
    {
        jclass local_cls_PhotoOptions = env->FindClass("nitro/nitro_camera_module/PhotoOptions");
        if (local_cls_PhotoOptions != nullptr) {
            g_cls_PhotoOptions = (jclass)env->NewGlobalRef(local_cls_PhotoOptions);
            env->DeleteLocalRef(local_cls_PhotoOptions);
            g_ctor_PhotoOptions = env->GetMethodID(g_cls_PhotoOptions, "<init>", "(JJJJJDDDJJ)V");
            g_fid_PhotoOptions_flash = env->GetFieldID(g_cls_PhotoOptions, "flash", "J");
            g_fid_PhotoOptions_qualityPrioritization = env->GetFieldID(g_cls_PhotoOptions, "qualityPrioritization", "J");
            g_fid_PhotoOptions_enableShutterSound = env->GetFieldID(g_cls_PhotoOptions, "enableShutterSound", "J");
            g_fid_PhotoOptions_skipMetadata = env->GetFieldID(g_cls_PhotoOptions, "skipMetadata", "J");
            g_fid_PhotoOptions_enableAutoRedEyeReduction = env->GetFieldID(g_cls_PhotoOptions, "enableAutoRedEyeReduction", "J");
            g_fid_PhotoOptions_latitude = env->GetFieldID(g_cls_PhotoOptions, "latitude", "D");
            g_fid_PhotoOptions_longitude = env->GetFieldID(g_cls_PhotoOptions, "longitude", "D");
            g_fid_PhotoOptions_altitude = env->GetFieldID(g_cls_PhotoOptions, "altitude", "D");
            g_fid_PhotoOptions_hasLocation = env->GetFieldID(g_cls_PhotoOptions, "hasLocation", "J");
            g_fid_PhotoOptions_outputFormat = env->GetFieldID(g_cls_PhotoOptions, "outputFormat", "J");
        }
    }
    {
        jclass local_cls_RecordingOptions = env->FindClass("nitro/nitro_camera_module/RecordingOptions");
        if (local_cls_RecordingOptions != nullptr) {
            g_cls_RecordingOptions = (jclass)env->NewGlobalRef(local_cls_RecordingOptions);
            env->DeleteLocalRef(local_cls_RecordingOptions);
            g_ctor_RecordingOptions = env->GetMethodID(g_cls_RecordingOptions, "<init>", "(JJJJJDDDJ)V");
            g_fid_RecordingOptions_codec = env->GetFieldID(g_cls_RecordingOptions, "codec", "J");
            g_fid_RecordingOptions_fileType = env->GetFieldID(g_cls_RecordingOptions, "fileType", "J");
            g_fid_RecordingOptions_bitRate = env->GetFieldID(g_cls_RecordingOptions, "bitRate", "J");
            g_fid_RecordingOptions_maxDurationMs = env->GetFieldID(g_cls_RecordingOptions, "maxDurationMs", "J");
            g_fid_RecordingOptions_maxFileSizeBytes = env->GetFieldID(g_cls_RecordingOptions, "maxFileSizeBytes", "J");
            g_fid_RecordingOptions_latitude = env->GetFieldID(g_cls_RecordingOptions, "latitude", "D");
            g_fid_RecordingOptions_longitude = env->GetFieldID(g_cls_RecordingOptions, "longitude", "D");
            g_fid_RecordingOptions_altitude = env->GetFieldID(g_cls_RecordingOptions, "altitude", "D");
            g_fid_RecordingOptions_hasLocation = env->GetFieldID(g_cls_RecordingOptions, "hasLocation", "J");
        }
    }
}

// ── postXxxToPort helpers for @nitroNativeAsync ──
JNIEXPORT void JNICALL Java_nitro_nitro_1camera_1module_NitroCameraJniBridge_postNullToPort(JNIEnv*, jclass, jlong dartPort) {
    Dart_CObject obj;
    obj.type = Dart_CObject_kNull;
    Dart_PostCObject_DL((Dart_Port)dartPort, &obj);
}

JNIEXPORT void JNICALL Java_nitro_nitro_1camera_1module_NitroCameraJniBridge_postInt64ToPort(JNIEnv*, jclass, jlong dartPort, jlong value) {
    Dart_CObject obj;
    obj.type = Dart_CObject_kInt64;
    obj.value.as_int64 = (int64_t)value;
    Dart_PostCObject_DL((Dart_Port)dartPort, &obj);
}

JNIEXPORT void JNICALL Java_nitro_nitro_1camera_1module_NitroCameraJniBridge_postDoubleToPort(JNIEnv*, jclass, jlong dartPort, jdouble value) {
    Dart_CObject obj;
    obj.type = Dart_CObject_kDouble;
    obj.value.as_double = (double)value;
    Dart_PostCObject_DL((Dart_Port)dartPort, &obj);
}

JNIEXPORT void JNICALL Java_nitro_nitro_1camera_1module_NitroCameraJniBridge_postBoolToPort(JNIEnv*, jclass, jlong dartPort, jboolean value) {
    Dart_CObject obj;
    obj.type = Dart_CObject_kBool;
    obj.value.as_bool = (bool)value;
    Dart_PostCObject_DL((Dart_Port)dartPort, &obj);
}

JNIEXPORT void JNICALL Java_nitro_nitro_1camera_1module_NitroCameraJniBridge_postStringToPort(JNIEnv* env, jclass, jlong dartPort, jstring value) {
    Dart_CObject obj;
    if (value == nullptr) {
        obj.type = Dart_CObject_kNull;
        Dart_PostCObject_DL((Dart_Port)dartPort, &obj);
        return;
    }
    const char* cStr = env->GetStringUTFChars(value, nullptr);
    if (cStr == nullptr) {
        obj.type = Dart_CObject_kNull;
        Dart_PostCObject_DL((Dart_Port)dartPort, &obj);
        return;
    }
    obj.type = Dart_CObject_kString;
    obj.value.as_string = const_cast<char*>(cStr);
    Dart_PostCObject_DL((Dart_Port)dartPort, &obj);
    env->ReleaseStringUTFChars(value, cStr);
}

JNIEXPORT void JNICALL Java_nitro_nitro_1camera_1module_NitroCameraJniBridge_postOptInt64ToPort(JNIEnv*, jclass, jlong dartPort, jlong value, jboolean hasValue) {
    uint8_t* buf = (uint8_t*)malloc(9);
    buf[0] = hasValue ? 1 : 0;
    if (hasValue) { memcpy(buf + 1, &value, 8); }
    Dart_CObject obj;
    obj.type = Dart_CObject_kInt64;
    obj.value.as_int64 = (int64_t)(uintptr_t)buf;
    Dart_PostCObject_DL((Dart_Port)dartPort, &obj);
}

JNIEXPORT void JNICALL Java_nitro_nitro_1camera_1module_NitroCameraJniBridge_postOptFloat64ToPort(JNIEnv*, jclass, jlong dartPort, jdouble value, jboolean hasValue) {
    uint8_t* buf = (uint8_t*)malloc(9);
    buf[0] = hasValue ? 1 : 0;
    if (hasValue) { memcpy(buf + 1, &value, 8); }
    Dart_CObject obj;
    obj.type = Dart_CObject_kInt64;
    obj.value.as_int64 = (int64_t)(uintptr_t)buf;
    Dart_PostCObject_DL((Dart_Port)dartPort, &obj);
}

JNIEXPORT void JNICALL Java_nitro_nitro_1camera_1module_NitroCameraJniBridge_postOptBoolToPort(JNIEnv*, jclass, jlong dartPort, jboolean value, jboolean hasValue) {
    uint8_t* buf = (uint8_t*)malloc(2);
    buf[0] = hasValue ? 1 : 0;
    buf[1] = value ? 1 : 0;
    Dart_CObject obj;
    obj.type = Dart_CObject_kInt64;
    obj.value.as_int64 = (int64_t)(uintptr_t)buf;
    Dart_PostCObject_DL((Dart_Port)dartPort, &obj);
}

JNIEXPORT void JNICALL Java_nitro_nitro_1camera_1module_NitroCameraJniBridge_postBytesToPort(JNIEnv* env, jclass, jlong dartPort, jbyteArray value) {
    Dart_CObject obj;
    obj.type = Dart_CObject_kInt64;
    if (value == nullptr) {
        obj.value.as_int64 = 0;
    } else {
        jsize len = env->GetArrayLength(value);
        uint8_t* buf = (uint8_t*)malloc(len);
        env->GetByteArrayRegion(value, 0, len, (jbyte*)buf);
        obj.value.as_int64 = (int64_t)(uintptr_t)buf;
    }
    Dart_PostCObject_DL((Dart_Port)dartPort, &obj);
}

JNIEXPORT void JNICALL Java_nitro_nitro_1camera_1module_NitroCameraJniBridge_postResolvedConfigToPort(JNIEnv* env, jclass, jlong dartPort, jobject value) {
    Dart_CObject obj;
    obj.type = Dart_CObject_kInt64;
    if (value == nullptr) {
        obj.value.as_int64 = 0;
    } else {
        ResolvedConfig* result = (ResolvedConfig*)malloc(sizeof(ResolvedConfig));
        *result = pack_ResolvedConfig_from_jni(env, value);
        obj.value.as_int64 = (int64_t)(uintptr_t)result;
    }
    Dart_PostCObject_DL((Dart_Port)dartPort, &obj);
}

JNIEXPORT void JNICALL Java_nitro_nitro_1camera_1module_NitroCameraJniBridge_reportNativeAsyncError(JNIEnv* env, jclass, jlong errPtr, jstring name, jstring message) {
    NitroError* err = (NitroError*)(uintptr_t)errPtr;
    if (err == nullptr) { return; }
    err->hasError = 1;
    if (name != nullptr) {
        const char* cName = env->GetStringUTFChars(name, nullptr);
        err->name = strdup(cName);
        env->ReleaseStringUTFChars(name, cName);
    } else {
        err->name = strdup("NativeException");
    }
    if (message != nullptr) {
        const char* cMsg = env->GetStringUTFChars(message, nullptr);
        err->message = strdup(cMsg);
        env->ReleaseStringUTFChars(message, cMsg);
    } else {
        err->message = strdup("An unknown native exception occurred.");
    }
    err->code = nullptr;
    err->stackTrace = nullptr;
}

} // extern "C"
#elif __APPLE__
extern "C" {
extern void _nitro_camera_call_requestCameraPermission(int64_t err_ptr, int64_t dart_port);
void nitro_camera_request_camera_permission(int64_t instanceId, NitroError* _nitro_err, int64_t dart_port) {
    nitro_camera_clear_error();
    _nitro_camera_call_requestCameraPermission((int64_t)(uintptr_t)_nitro_err, dart_port);
}

extern int64_t _nitro_camera_call_getCameraPermissionStatus(void);
int64_t nitro_camera_get_camera_permission_status(int64_t instanceId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        return _nitro_camera_call_getCameraPermissionStatus();
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
        return 0;
    }
#else
    return _nitro_camera_call_getCameraPermissionStatus();
#endif
}

extern void _nitro_camera_call_requestMicrophonePermission(int64_t err_ptr, int64_t dart_port);
void nitro_camera_request_microphone_permission(int64_t instanceId, NitroError* _nitro_err, int64_t dart_port) {
    nitro_camera_clear_error();
    _nitro_camera_call_requestMicrophonePermission((int64_t)(uintptr_t)_nitro_err, dart_port);
}

extern int64_t _nitro_camera_call_getMicrophonePermissionStatus(void);
int64_t nitro_camera_get_microphone_permission_status(int64_t instanceId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        return _nitro_camera_call_getMicrophonePermissionStatus();
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
        return 0;
    }
#else
    return _nitro_camera_call_getMicrophonePermissionStatus();
#endif
}

extern void _nitro_camera_call_getAvailableCameraDevicesJson(int64_t err_ptr, int64_t dart_port);
void nitro_camera_get_available_camera_devices_json(int64_t instanceId, NitroError* _nitro_err, int64_t dart_port) {
    nitro_camera_clear_error();
    _nitro_camera_call_getAvailableCameraDevicesJson((int64_t)(uintptr_t)_nitro_err, dart_port);
}

extern void* _nitro_camera_call_getAvailableCameraDevices(void);
void* nitro_camera_get_available_camera_devices(int64_t instanceId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        return _nitro_camera_call_getAvailableCameraDevices();
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
        return nullptr;
    }
#else
    return _nitro_camera_call_getAvailableCameraDevices();
#endif
}

extern int64_t _nitro_camera_call_getDeviceCount(void);
int64_t nitro_camera_get_device_count(int64_t instanceId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        return _nitro_camera_call_getDeviceCount();
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
        return 0;
    }
#else
    return _nitro_camera_call_getDeviceCount();
#endif
}

extern void* _nitro_camera_call_getDevice(int64_t index);
void* nitro_camera_get_device(int64_t instanceId, int64_t index, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        return _nitro_camera_call_getDevice(index);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
        return nullptr;
    }
#else
    return _nitro_camera_call_getDevice(index);
#endif
}

extern void _nitro_camera_call_openCamera(const char* deviceId, int64_t width, int64_t height, int64_t fps, int64_t enableAudio, int64_t err_ptr, int64_t dart_port);
void nitro_camera_open_camera(int64_t instanceId, const char* deviceId, int64_t width, int64_t height, int64_t fps, int64_t enableAudio, NitroError* _nitro_err, int64_t dart_port) {
    nitro_camera_clear_error();
    _nitro_camera_call_openCamera(deviceId, width, height, fps, enableAudio, (int64_t)(uintptr_t)_nitro_err, dart_port);
}

extern void _nitro_camera_call_closeCamera(int64_t textureId, int64_t err_ptr, int64_t dart_port);
void nitro_camera_close_camera(int64_t instanceId, int64_t textureId, NitroError* _nitro_err, int64_t dart_port) {
    nitro_camera_clear_error();
    _nitro_camera_call_closeCamera(textureId, (int64_t)(uintptr_t)_nitro_err, dart_port);
}

extern void _nitro_camera_call_startPreview(int64_t textureId);
void nitro_camera_start_preview(int64_t instanceId, int64_t textureId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_startPreview(textureId);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_startPreview(textureId);
#endif
}

extern void _nitro_camera_call_stopPreview(int64_t textureId);
void nitro_camera_stop_preview(int64_t instanceId, int64_t textureId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_stopPreview(textureId);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_stopPreview(textureId);
#endif
}

extern void _nitro_camera_call_setZoom(int64_t textureId, double zoom);
void nitro_camera_set_zoom(int64_t instanceId, int64_t textureId, double zoom, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_setZoom(textureId, zoom);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_setZoom(textureId, zoom);
#endif
}

extern void _nitro_camera_call_setFocusPoint(int64_t textureId, double x, double y);
void nitro_camera_set_focus_point(int64_t instanceId, int64_t textureId, double x, double y, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_setFocusPoint(textureId, x, y);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_setFocusPoint(textureId, x, y);
#endif
}

extern void _nitro_camera_call_setAutoFocus(int64_t textureId, int64_t mode);
void nitro_camera_set_auto_focus(int64_t instanceId, int64_t textureId, int64_t mode, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_setAutoFocus(textureId, mode);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_setAutoFocus(textureId, mode);
#endif
}

extern void _nitro_camera_call_setExposure(int64_t textureId, double value);
void nitro_camera_set_exposure(int64_t instanceId, int64_t textureId, double value, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_setExposure(textureId, value);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_setExposure(textureId, value);
#endif
}

extern void _nitro_camera_call_setFlash(int64_t textureId, int64_t mode);
void nitro_camera_set_flash(int64_t instanceId, int64_t textureId, int64_t mode, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_setFlash(textureId, mode);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_setFlash(textureId, mode);
#endif
}

extern void _nitro_camera_call_setTorch(int64_t textureId, int64_t enabled);
void nitro_camera_set_torch(int64_t instanceId, int64_t textureId, int64_t enabled, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_setTorch(textureId, enabled);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_setTorch(textureId, enabled);
#endif
}

extern void _nitro_camera_call_setWhiteBalance(int64_t textureId, int64_t temperature);
void nitro_camera_set_white_balance(int64_t instanceId, int64_t textureId, int64_t temperature, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_setWhiteBalance(textureId, temperature);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_setWhiteBalance(textureId, temperature);
#endif
}

extern void _nitro_camera_call_setHdr(int64_t textureId, int64_t enabled);
void nitro_camera_set_hdr(int64_t instanceId, int64_t textureId, int64_t enabled, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_setHdr(textureId, enabled);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_setHdr(textureId, enabled);
#endif
}

extern void _nitro_camera_call_takePhoto(int64_t textureId, int64_t err_ptr, int64_t dart_port);
void nitro_camera_take_photo(int64_t instanceId, int64_t textureId, NitroError* _nitro_err, int64_t dart_port) {
    nitro_camera_clear_error();
    _nitro_camera_call_takePhoto(textureId, (int64_t)(uintptr_t)_nitro_err, dart_port);
}

extern void _nitro_camera_call_startVideoRecording(int64_t textureId, const char* outputPath, void* options, int64_t err_ptr, int64_t dart_port);
void nitro_camera_start_video_recording(int64_t instanceId, int64_t textureId, const char* outputPath, void* options, NitroError* _nitro_err, int64_t dart_port) {
    nitro_camera_clear_error();
    _nitro_camera_call_startVideoRecording(textureId, outputPath, options, (int64_t)(uintptr_t)_nitro_err, dart_port);
}

extern void _nitro_camera_call_stopVideoRecording(int64_t textureId, int64_t err_ptr, int64_t dart_port);
void nitro_camera_stop_video_recording(int64_t instanceId, int64_t textureId, NitroError* _nitro_err, int64_t dart_port) {
    nitro_camera_clear_error();
    _nitro_camera_call_stopVideoRecording(textureId, (int64_t)(uintptr_t)_nitro_err, dart_port);
}

extern void _nitro_camera_call_pauseRecording(int64_t textureId);
void nitro_camera_pause_recording(int64_t instanceId, int64_t textureId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_pauseRecording(textureId);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_pauseRecording(textureId);
#endif
}

extern void _nitro_camera_call_resumeRecording(int64_t textureId);
void nitro_camera_resume_recording(int64_t instanceId, int64_t textureId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_resumeRecording(textureId);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_resumeRecording(textureId);
#endif
}

extern void _nitro_camera_call_cancelRecording(int64_t textureId);
void nitro_camera_cancel_recording(int64_t instanceId, int64_t textureId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_cancelRecording(textureId);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_cancelRecording(textureId);
#endif
}

extern void _nitro_camera_call_enableFrameProcessing(int64_t textureId, int64_t enabled);
void nitro_camera_enable_frame_processing(int64_t instanceId, int64_t textureId, int64_t enabled, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_enableFrameProcessing(textureId, enabled);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_enableFrameProcessing(textureId, enabled);
#endif
}

extern void _nitro_camera_call_setFrameFormat(int64_t textureId, int64_t format);
void nitro_camera_set_frame_format(int64_t instanceId, int64_t textureId, int64_t format, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_setFrameFormat(textureId, format);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_setFrameFormat(textureId, format);
#endif
}

extern void _nitro_camera_call_setSamplingRate(int64_t textureId, int64_t samplingRate);
void nitro_camera_set_sampling_rate(int64_t instanceId, int64_t textureId, int64_t samplingRate, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_setSamplingRate(textureId, samplingRate);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_setSamplingRate(textureId, samplingRate);
#endif
}

extern void _nitro_camera_call_setFilterShader(int64_t textureId, const char* shaderSource);
void nitro_camera_set_filter_shader(int64_t instanceId, int64_t textureId, const char* shaderSource, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_setFilterShader(textureId, shaderSource);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_setFilterShader(textureId, shaderSource);
#endif
}

extern void _nitro_camera_call_updateOverlay(int64_t textureId, uint8_t* overlayData, size_t overlayData_length);
void nitro_camera_update_overlay(int64_t instanceId, int64_t textureId, uint8_t* overlayData, size_t overlayData_length, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_updateOverlay(textureId, overlayData, overlayData_length);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_updateOverlay(textureId, overlayData, overlayData_length);
#endif
}

extern void _nitro_camera_call_configure(int64_t textureId, void* config, int64_t err_ptr, int64_t dart_port);
void nitro_camera_configure(int64_t instanceId, int64_t textureId, void* config, NitroError* _nitro_err, int64_t dart_port) {
    nitro_camera_clear_error();
    _nitro_camera_call_configure(textureId, config, (int64_t)(uintptr_t)_nitro_err, dart_port);
}

extern const char* _nitro_camera_call_getSessionStateJson(int64_t textureId);
const char* nitro_camera_get_session_state_json(int64_t instanceId, int64_t textureId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        return _nitro_camera_call_getSessionStateJson(textureId);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
        return nullptr;
    }
#else
    return _nitro_camera_call_getSessionStateJson(textureId);
#endif
}

extern void _nitro_camera_call_setVideoStabilization(int64_t textureId, int64_t mode);
void nitro_camera_set_video_stabilization(int64_t instanceId, int64_t textureId, int64_t mode, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_setVideoStabilization(textureId, mode);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_setVideoStabilization(textureId, mode);
#endif
}

extern void _nitro_camera_call_setLowLightBoost(int64_t textureId, int64_t enabled);
void nitro_camera_set_low_light_boost(int64_t instanceId, int64_t textureId, int64_t enabled, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_setLowLightBoost(textureId, enabled);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_setLowLightBoost(textureId, enabled);
#endif
}

extern void _nitro_camera_call_setTorchLevel(int64_t textureId, double level);
void nitro_camera_set_torch_level(int64_t instanceId, int64_t textureId, double level, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_setTorchLevel(textureId, level);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_setTorchLevel(textureId, level);
#endif
}

extern void _nitro_camera_call_lockExposure(int64_t textureId, int64_t locked);
void nitro_camera_lock_exposure(int64_t instanceId, int64_t textureId, int64_t locked, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_lockExposure(textureId, locked);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_lockExposure(textureId, locked);
#endif
}

extern void _nitro_camera_call_lockFocus(int64_t textureId, int64_t locked);
void nitro_camera_lock_focus(int64_t instanceId, int64_t textureId, int64_t locked, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_lockFocus(textureId, locked);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_lockFocus(textureId, locked);
#endif
}

extern void _nitro_camera_call_lockWhiteBalance(int64_t textureId, int64_t locked);
void nitro_camera_lock_white_balance(int64_t instanceId, int64_t textureId, int64_t locked, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_lockWhiteBalance(textureId, locked);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_lockWhiteBalance(textureId, locked);
#endif
}

extern void _nitro_camera_call_setTargetOrientation(int64_t textureId, int64_t degrees);
void nitro_camera_set_target_orientation(int64_t instanceId, int64_t textureId, int64_t degrees, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_setTargetOrientation(textureId, degrees);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_setTargetOrientation(textureId, degrees);
#endif
}

extern void _nitro_camera_call_setDistortionCorrection(int64_t textureId, int64_t enabled);
void nitro_camera_set_distortion_correction(int64_t instanceId, int64_t textureId, int64_t enabled, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_setDistortionCorrection(textureId, enabled);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_setDistortionCorrection(textureId, enabled);
#endif
}

extern void _nitro_camera_call_enableOrientationEvents(int64_t enabled);
void nitro_camera_enable_orientation_events(int64_t instanceId, int64_t enabled, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_enableOrientationEvents(enabled);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_enableOrientationEvents(enabled);
#endif
}

extern void _nitro_camera_call_enableDeviceAvailabilityEvents(int64_t enabled);
void nitro_camera_enable_device_availability_events(int64_t instanceId, int64_t enabled, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_enableDeviceAvailabilityEvents(enabled);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_enableDeviceAvailabilityEvents(enabled);
#endif
}

extern void _nitro_camera_call_setNativeDetector(int64_t textureId, const char* detector);
void nitro_camera_set_native_detector(int64_t instanceId, int64_t textureId, const char* detector, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_setNativeDetector(textureId, detector);
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_setNativeDetector(textureId, detector);
#endif
}

extern const char* _nitro_camera_call_getConcurrentCameraIdsJson(void);
const char* nitro_camera_get_concurrent_camera_ids_json(int64_t instanceId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        return _nitro_camera_call_getConcurrentCameraIdsJson();
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
        return nullptr;
    }
#else
    return _nitro_camera_call_getConcurrentCameraIdsJson();
#endif
}

extern void _nitro_camera_call_takePhotoWithOptions(int64_t textureId, void* options, int64_t err_ptr, int64_t dart_port);
void nitro_camera_take_photo_with_options(int64_t instanceId, int64_t textureId, void* options, NitroError* _nitro_err, int64_t dart_port) {
    nitro_camera_clear_error();
    _nitro_camera_call_takePhotoWithOptions(textureId, options, (int64_t)(uintptr_t)_nitro_err, dart_port);
}

extern void _nitro_camera_call_takeSnapshot(int64_t textureId, int64_t err_ptr, int64_t dart_port);
void nitro_camera_take_snapshot(int64_t instanceId, int64_t textureId, NitroError* _nitro_err, int64_t dart_port) {
    nitro_camera_clear_error();
    _nitro_camera_call_takeSnapshot(textureId, (int64_t)(uintptr_t)_nitro_err, dart_port);
}

extern void _nitro_camera_call_reset(void);
void nitro_camera_reset(int64_t instanceId, NitroError* _nitro_err) {
    if (_nitro_err) { _nitro_err->hasError = 0; }
#ifdef __OBJC__
    @try {
        _nitro_camera_call_reset();
    } @catch (NSException* e) {
        if (_nitro_err) {
            // sync: write exception to out-param error slot.
            _nitro_err->hasError = 1;
            _nitro_err->name    = strdup([e.name UTF8String]);
            _nitro_err->message = strdup([e.reason UTF8String]);
            _nitro_err->code = nullptr;
            _nitro_err->stackTrace = nullptr;
        } else {
            // async: _nitro_err is null — route exception to TLS slot.
            nitro_report_error([e.name UTF8String], [e.reason UTF8String], nullptr, nullptr);
        }
    }
#else
    _nitro_camera_call_reset();
#endif
}

bool _emit_frameStream_to_dart(int64_t dartPort, void* item) {
    Dart_CObject obj;
    obj.type = Dart_CObject_kInt64;
    obj.value.as_int64 = (intptr_t)item;
    return Dart_PostCObject_DL(dartPort, &obj);
}

extern void _nitro_camera_register_frameStream_stream(int64_t dartPort, bool (*emitCb)(int64_t, void*));
void nitro_camera_register_frame_stream_stream(int64_t instanceId, int64_t dart_port) {
    _nitro_camera_register_frameStream_stream(dart_port, _emit_frameStream_to_dart);
}
extern void _nitro_camera_release_frameStream_stream(int64_t dart_port);
void nitro_camera_release_frame_stream_stream(int64_t dart_port) {
    _nitro_camera_release_frameStream_stream(dart_port);
}

bool _emit_eventStream_to_dart(int64_t dartPort, void* item) {
    Dart_CObject obj;
    obj.type = Dart_CObject_kInt64;
    obj.value.as_int64 = (intptr_t)item;
    return Dart_PostCObject_DL(dartPort, &obj);
}

extern void _nitro_camera_register_eventStream_stream(int64_t dartPort, bool (*emitCb)(int64_t, void*));
void nitro_camera_register_event_stream_stream(int64_t instanceId, int64_t dart_port) {
    _nitro_camera_register_eventStream_stream(dart_port, _emit_eventStream_to_dart);
}
extern void _nitro_camera_release_eventStream_stream(int64_t dart_port);
void nitro_camera_release_event_stream_stream(int64_t dart_port) {
    _nitro_camera_release_eventStream_stream(dart_port);
}

static int64_t nitro_camera_g_next_instance_id = 0;
NITRO_EXPORT int64_t nitro_camera_create_instance(const char* key) { (void)key; return nitro_camera_g_next_instance_id++; }
NITRO_EXPORT void nitro_camera_destroy_instance(int64_t instanceId) { (void)instanceId; }

} // extern "C"
#endif

# Run with `dart run ffigen --config ffigen.yaml`.
name: NitroCameraBindings
description: |
  Bindings for `src/nitro_camera.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/nitro_camera_bindings_generated.dart'
headers:
  entry-points:
    - 'src/nitro_camera.h'
  include-directives:
    - 'src/nitro_camera.h'
comments:
  style: any
  length: full

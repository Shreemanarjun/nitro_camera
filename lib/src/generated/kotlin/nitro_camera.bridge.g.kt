// Generated by Nitrogen Modules. Do not edit.
// nitro_generator: 0.5.9
// Generated from: nitro_camera.native.dart
package nitro.nitro_camera_module

import android.app.Activity
import android.content.Context
import androidx.annotation.Keep
import kotlinx.coroutines.flow.Flow
import kotlinx.coroutines.launch
import kotlinx.coroutines.CoroutineScope
import kotlinx.coroutines.CoroutineStart
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.flow.buffer
import kotlinx.coroutines.runBlocking

// --- Enums ---
@Keep
enum class CameraPosition(val nativeValue: Long) {
  FRONT(0),
  BACK(1),
  EXTERNAL(2);

  companion object {
    fun fromNative(v: Long): CameraPosition = values().first { it.nativeValue == v }
  }
}

@Keep
enum class CameraLensType(val nativeValue: Long) {
  UNKNOWN(0),
  WIDEANGLE(1),
  ULTRAWIDEANGLE(2),
  TELEPHOTO(3);

  companion object {
    fun fromNative(v: Long): CameraLensType = values().first { it.nativeValue == v }
  }
}

@Keep
enum class FlashMode(val nativeValue: Long) {
  OFF(0),
  ON(1),
  AUTO(2);

  companion object {
    fun fromNative(v: Long): FlashMode = values().first { it.nativeValue == v }
  }
}

@Keep
enum class AutoFocusMode(val nativeValue: Long) {
  OFF(0),
  CONTINUOUS(1),
  LOCKED(2);

  companion object {
    fun fromNative(v: Long): AutoFocusMode = values().first { it.nativeValue == v }
  }
}

@Keep
enum class PermissionStatus(val nativeValue: Long) {
  NOTDETERMINED(0),
  GRANTED(1),
  DENIED(2),
  RESTRICTED(3);

  companion object {
    fun fromNative(v: Long): PermissionStatus = values().first { it.nativeValue == v }
  }
}

@Keep
enum class VideoStabilizationMode(val nativeValue: Long) {
  OFF(0),
  STANDARD(1),
  CINEMATIC(2),
  CINEMATICEXTENDED(3),
  AUTO(4);

  companion object {
    fun fromNative(v: Long): VideoStabilizationMode = values().first { it.nativeValue == v }
  }
}

@Keep
enum class QualityPrioritization(val nativeValue: Long) {
  SPEED(0),
  BALANCED(1),
  QUALITY(2);

  companion object {
    fun fromNative(v: Long): QualityPrioritization = values().first { it.nativeValue == v }
  }
}

@Keep
enum class VideoCodec(val nativeValue: Long) {
  H264(0),
  HEVC(1);

  companion object {
    fun fromNative(v: Long): VideoCodec = values().first { it.nativeValue == v }
  }
}

@Keep
enum class VideoFileType(val nativeValue: Long) {
  MP4(0),
  MOV(1);

  companion object {
    fun fromNative(v: Long): VideoFileType = values().first { it.nativeValue == v }
  }
}

@Keep
enum class CameraEventType(val nativeValue: Long) {
  STARTED(0),
  STOPPED(1),
  ERROR(2),
  INTERRUPTIONSTARTED(3),
  INTERRUPTIONENDED(4),
  FRAMEDROPPED(5),
  PHOTOCAPTUREBEGAN(6),
  PHOTOCAPTURESHUTTER(7),
  PHOTOTHUMBNAIL(8),
  DEVICECONNECTED(9),
  DEVICEDISCONNECTED(10),
  ORIENTATIONCHANGED(11),
  DETECTION(12),
  THERMALSTATECHANGED(13);

  companion object {
    fun fromNative(v: Long): CameraEventType = values().first { it.nativeValue == v }
  }
}

@Keep
enum class InterruptionReason(val nativeValue: Long) {
  NONE(0),
  VIDEODEVICENOTAVAILABLEINBACKGROUND(1),
  AUDIODEVICEINUSEBYANOTHERCLIENT(2),
  VIDEODEVICEINUSEBYANOTHERCLIENT(3),
  VIDEODEVICENOTAVAILABLEWITHMULTIPLEFOREGROUNDAPPS(4),
  VIDEODEVICENOTAVAILABLEDUETOSYSTEMPRESSURE(5),
  UNKNOWN(6);

  companion object {
    fun fromNative(v: Long): InterruptionReason = values().first { it.nativeValue == v }
  }
}

// --- Structs ---
@androidx.annotation.Keep
data class CameraFrame(val pixels: java.nio.ByteBuffer, val size: Long, val width: Long, val height: Long, val timestamp: Long, val orientation: Long, val textureId: Long, val bytesPerRow: Long, val pixelFormat: Long, val isMirrored: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): CameraFrame {
            val pixels = { buf.long; java.nio.ByteBuffer.allocate(0) }()
            val size = buf.long
            val width = buf.long
            val height = buf.long
            val timestamp = buf.long
            val orientation = buf.long
            val textureId = buf.long
            val bytesPerRow = buf.long
            val pixelFormat = buf.long
            val isMirrored = buf.long
            return CameraFrame(pixels, size, width, height, timestamp, orientation, textureId, bytesPerRow, pixelFormat, isMirrored)
        }
        @JvmStatic fun decode(bytes: ByteArray): CameraFrame {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        @Suppress("UNUSED_PARAMETER") fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(0L)
        writeInt(size)
        writeInt(width)
        writeInt(height)
        writeInt(timestamp)
        writeInt(orientation)
        writeInt(textureId)
        writeInt(bytesPerRow)
        writeInt(pixelFormat)
        writeInt(isMirrored)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(108)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        return out.toByteArray()
    }
}

@androidx.annotation.Keep
data class CameraConfig(val zoom: Double, val exposure: Double, val flash: Long, val torch: Long, val torchLevel: Double, val whiteBalanceKelvin: Long, val videoHdr: Long, val lowLightBoost: Long, val autoFocus: Long, val videoStabilization: Long, val active: Long, val enableFrameProcessing: Long, val pixelFormat: Long, val samplingRate: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): CameraConfig {
            val zoom = buf.double
            val exposure = buf.double
            val flash = buf.long
            val torch = buf.long
            val torchLevel = buf.double
            val whiteBalanceKelvin = buf.long
            val videoHdr = buf.long
            val lowLightBoost = buf.long
            val autoFocus = buf.long
            val videoStabilization = buf.long
            val active = buf.long
            val enableFrameProcessing = buf.long
            val pixelFormat = buf.long
            val samplingRate = buf.long
            return CameraConfig(zoom, exposure, flash, torch, torchLevel, whiteBalanceKelvin, videoHdr, lowLightBoost, autoFocus, videoStabilization, active, enableFrameProcessing, pixelFormat, samplingRate)
        }
        @JvmStatic fun decode(bytes: ByteArray): CameraConfig {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        @Suppress("UNUSED_PARAMETER") fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeDouble(zoom)
        writeDouble(exposure)
        writeInt(flash)
        writeInt(torch)
        writeDouble(torchLevel)
        writeInt(whiteBalanceKelvin)
        writeInt(videoHdr)
        writeInt(lowLightBoost)
        writeInt(autoFocus)
        writeInt(videoStabilization)
        writeInt(active)
        writeInt(enableFrameProcessing)
        writeInt(pixelFormat)
        writeInt(samplingRate)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(112)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        return out.toByteArray()
    }
}

@androidx.annotation.Keep
data class ResolvedConfig(val width: Long, val height: Long, val fps: Long, val pixelFormat: Long, val videoHdrEnabled: Long, val autoFocusSystem: Long, val active: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): ResolvedConfig {
            val width = buf.long
            val height = buf.long
            val fps = buf.long
            val pixelFormat = buf.long
            val videoHdrEnabled = buf.long
            val autoFocusSystem = buf.long
            val active = buf.long
            return ResolvedConfig(width, height, fps, pixelFormat, videoHdrEnabled, autoFocusSystem, active)
        }
        @JvmStatic fun decode(bytes: ByteArray): ResolvedConfig {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        @Suppress("UNUSED_PARAMETER") fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(width)
        writeInt(height)
        writeInt(fps)
        writeInt(pixelFormat)
        writeInt(videoHdrEnabled)
        writeInt(autoFocusSystem)
        writeInt(active)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(56)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        return out.toByteArray()
    }
}

@androidx.annotation.Keep
data class PhotoOptions(val flash: Long, val qualityPrioritization: Long, val enableShutterSound: Long, val skipMetadata: Long, val enableAutoRedEyeReduction: Long, val latitude: Double, val longitude: Double, val altitude: Double, val hasLocation: Long, val outputFormat: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PhotoOptions {
            val flash = buf.long
            val qualityPrioritization = buf.long
            val enableShutterSound = buf.long
            val skipMetadata = buf.long
            val enableAutoRedEyeReduction = buf.long
            val latitude = buf.double
            val longitude = buf.double
            val altitude = buf.double
            val hasLocation = buf.long
            val outputFormat = buf.long
            return PhotoOptions(flash, qualityPrioritization, enableShutterSound, skipMetadata, enableAutoRedEyeReduction, latitude, longitude, altitude, hasLocation, outputFormat)
        }
        @JvmStatic fun decode(bytes: ByteArray): PhotoOptions {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        @Suppress("UNUSED_PARAMETER") fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(flash)
        writeInt(qualityPrioritization)
        writeInt(enableShutterSound)
        writeInt(skipMetadata)
        writeInt(enableAutoRedEyeReduction)
        writeDouble(latitude)
        writeDouble(longitude)
        writeDouble(altitude)
        writeInt(hasLocation)
        writeInt(outputFormat)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(80)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        return out.toByteArray()
    }
}

@androidx.annotation.Keep
data class RecordingOptions(val codec: Long, val fileType: Long, val bitRate: Long, val maxDurationMs: Long, val maxFileSizeBytes: Long, val latitude: Double, val longitude: Double, val altitude: Double, val hasLocation: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RecordingOptions {
            val codec = buf.long
            val fileType = buf.long
            val bitRate = buf.long
            val maxDurationMs = buf.long
            val maxFileSizeBytes = buf.long
            val latitude = buf.double
            val longitude = buf.double
            val altitude = buf.double
            val hasLocation = buf.long
            return RecordingOptions(codec, fileType, bitRate, maxDurationMs, maxFileSizeBytes, latitude, longitude, altitude, hasLocation)
        }
        @JvmStatic fun decode(bytes: ByteArray): RecordingOptions {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        @Suppress("UNUSED_PARAMETER") fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(codec)
        writeInt(fileType)
        writeInt(bitRate)
        writeInt(maxDurationMs)
        writeInt(maxFileSizeBytes)
        writeDouble(latitude)
        writeDouble(longitude)
        writeDouble(altitude)
        writeInt(hasLocation)
    }

    fun encode(): ByteArray {
        val out = java.io.ByteArrayOutputStream(72)
        val buf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        writeFieldsTo(out, buf)
        return out.toByteArray()
    }
}

// --- @HybridRecord Kotlin data classes (generated by Nitrogen) ---

@androidx.annotation.Keep
data class CameraDevice(val id: String, val name: String, val position: Long, val lensType: Long, val sensorOrientation: Long, val minZoom: Double, val maxZoom: Double, val neutralZoom: Double, val hasFlash: Long, val hasTorch: Long, val maxPhotoWidth: Long, val maxPhotoHeight: Long, val focalLength: Double, val aperture: Double) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): CameraDevice {
            val id = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val name = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val position = buf.long
            val lensType = buf.long
            val sensorOrientation = buf.long
            val minZoom = buf.double
            val maxZoom = buf.double
            val neutralZoom = buf.double
            val hasFlash = buf.long
            val hasTorch = buf.long
            val maxPhotoWidth = buf.long
            val maxPhotoHeight = buf.long
            val focalLength = buf.double
            val aperture = buf.double
            return CameraDevice(id, name, position, lensType, sensorOrientation, minZoom, maxZoom, neutralZoom, hasFlash, hasTorch, maxPhotoWidth, maxPhotoHeight, focalLength, aperture)
        }
        @JvmStatic fun decode(bytes: ByteArray): CameraDevice {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): CameraDevice = CameraDevice(
                id = map["id"] as String,
                name = map["name"] as String,
                position = (map["position"] as Number).toLong(),
                lensType = (map["lensType"] as Number).toLong(),
                sensorOrientation = (map["sensorOrientation"] as Number).toLong(),
                minZoom = (map["minZoom"] as Number).toDouble(),
                maxZoom = (map["maxZoom"] as Number).toDouble(),
                neutralZoom = (map["neutralZoom"] as Number).toDouble(),
                hasFlash = (map["hasFlash"] as Number).toLong(),
                hasTorch = (map["hasTorch"] as Number).toLong(),
                maxPhotoWidth = (map["maxPhotoWidth"] as Number).toLong(),
                maxPhotoHeight = (map["maxPhotoHeight"] as Number).toLong(),
                focalLength = (map["focalLength"] as Number).toDouble(),
                aperture = (map["aperture"] as Number).toDouble()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(168) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(id)
        writeString(name)
        writeInt(position.toLong())
        writeInt(lensType.toLong())
        writeInt(sensorOrientation.toLong())
        writeDouble(minZoom)
        writeDouble(maxZoom)
        writeDouble(neutralZoom)
        writeInt(hasFlash.toLong())
        writeInt(hasTorch.toLong())
        writeInt(maxPhotoWidth.toLong())
        writeInt(maxPhotoHeight.toLong())
        writeDouble(focalLength)
        writeDouble(aperture)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, CameraDevice> support ---
    fun toJson(): Map<String, Any?> = mapOf("id" to id, "name" to name, "position" to position, "lensType" to lensType, "sensorOrientation" to sensorOrientation, "minZoom" to minZoom, "maxZoom" to maxZoom, "neutralZoom" to neutralZoom, "hasFlash" to hasFlash, "hasTorch" to hasTorch, "maxPhotoWidth" to maxPhotoWidth, "maxPhotoHeight" to maxPhotoHeight, "focalLength" to focalLength, "aperture" to aperture)

}

@androidx.annotation.Keep
data class PhotoResult(val path: String, val width: Long, val height: Long, val fileSize: Long, val orientation: Long, val isMirrored: Long, val timestamp: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): PhotoResult {
            val path = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val width = buf.long
            val height = buf.long
            val fileSize = buf.long
            val orientation = buf.long
            val isMirrored = buf.long
            val timestamp = buf.long
            return PhotoResult(path, width, height, fileSize, orientation, isMirrored, timestamp)
        }
        @JvmStatic fun decode(bytes: ByteArray): PhotoResult {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): PhotoResult = PhotoResult(
                path = map["path"] as String,
                width = (map["width"] as Number).toLong(),
                height = (map["height"] as Number).toLong(),
                fileSize = (map["fileSize"] as Number).toLong(),
                orientation = (map["orientation"] as Number).toLong(),
                isMirrored = (map["isMirrored"] as Number).toLong(),
                timestamp = (map["timestamp"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(84) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(path)
        writeInt(width.toLong())
        writeInt(height.toLong())
        writeInt(fileSize.toLong())
        writeInt(orientation.toLong())
        writeInt(isMirrored.toLong())
        writeInt(timestamp.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, PhotoResult> support ---
    fun toJson(): Map<String, Any?> = mapOf("path" to path, "width" to width, "height" to height, "fileSize" to fileSize, "orientation" to orientation, "isMirrored" to isMirrored, "timestamp" to timestamp)

}

@androidx.annotation.Keep
data class RecordingResult(val path: String, val durationMs: Long, val fileSize: Long, val width: Long, val height: Long, val codec: Long, val fileType: Long, val finishedReason: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): RecordingResult {
            val path = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            val durationMs = buf.long
            val fileSize = buf.long
            val width = buf.long
            val height = buf.long
            val codec = buf.long
            val fileType = buf.long
            val finishedReason = buf.long
            return RecordingResult(path, durationMs, fileSize, width, height, codec, fileType, finishedReason)
        }
        @JvmStatic fun decode(bytes: ByteArray): RecordingResult {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): RecordingResult = RecordingResult(
                path = map["path"] as String,
                durationMs = (map["durationMs"] as Number).toLong(),
                fileSize = (map["fileSize"] as Number).toLong(),
                width = (map["width"] as Number).toLong(),
                height = (map["height"] as Number).toLong(),
                codec = (map["codec"] as Number).toLong(),
                fileType = (map["fileType"] as Number).toLong(),
                finishedReason = (map["finishedReason"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(92) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeString(path)
        writeInt(durationMs.toLong())
        writeInt(fileSize.toLong())
        writeInt(width.toLong())
        writeInt(height.toLong())
        writeInt(codec.toLong())
        writeInt(fileType.toLong())
        writeInt(finishedReason.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, RecordingResult> support ---
    fun toJson(): Map<String, Any?> = mapOf("path" to path, "durationMs" to durationMs, "fileSize" to fileSize, "width" to width, "height" to height, "codec" to codec, "fileType" to fileType, "finishedReason" to finishedReason)

}

@androidx.annotation.Keep
data class CameraEvent(val type: Long, val textureId: Long, val reason: Long, val message: String) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): CameraEvent {
            val type = buf.long
            val textureId = buf.long
            val reason = buf.long
            val message = { val len = buf.int; val b = ByteArray(len); buf.get(b); b.toString(Charsets.UTF_8) }()
            return CameraEvent(type, textureId, reason, message)
        }
        @JvmStatic fun decode(bytes: ByteArray): CameraEvent {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): CameraEvent = CameraEvent(
                type = (map["type"] as Number).toLong(),
                textureId = (map["textureId"] as Number).toLong(),
                reason = (map["reason"] as Number).toLong(),
                message = map["message"] as String
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(60) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(type.toLong())
        writeInt(textureId.toLong())
        writeInt(reason.toLong())
        writeString(message)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, CameraEvent> support ---
    fun toJson(): Map<String, Any?> = mapOf("type" to type, "textureId" to textureId, "reason" to reason, "message" to message)

}

@androidx.annotation.Keep
data class NitroNullableInt(val hasValue: Boolean, val value: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableInt {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.long
            return NitroNullableInt(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableInt {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroNullableInt = NitroNullableInt(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Long?) : this(v != null, v ?: 0L)
    val nullable: Long? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeInt(value.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, NitroNullableInt> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroNullableDouble(val hasValue: Boolean, val value: Double) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableDouble {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.double
            return NitroNullableDouble(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableDouble {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroNullableDouble = NitroNullableDouble(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toDouble()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Double?) : this(v != null, v ?: 0.0)
    val nullable: Double? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeDouble(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, NitroNullableDouble> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroNullableBool(val hasValue: Boolean, val value: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableBool {
            val hasValue = (buf.get().toInt() != 0)
            val value = (buf.get().toInt() != 0)
            return NitroNullableBool(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableBool {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroNullableBool = NitroNullableBool(
                hasValue = map["hasValue"] as Boolean,
                value = map["value"] as Boolean
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(2) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Boolean?) : this(v != null, v ?: false)
    val nullable: Boolean? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeBool(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, NitroNullableBool> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroOptInt64(val hasValue: Boolean, val value: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroOptInt64 {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.long
            return NitroOptInt64(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroOptInt64 {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroOptInt64 = NitroOptInt64(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Long?) : this(v != null, v ?: 0L)
    val nullable: Long? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeInt(value.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        return out.toByteArray()  // NO 4-byte length prefix
    }

    // --- toJson/fromJson for Map<String, NitroOptInt64> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroOptFloat64(val hasValue: Boolean, val value: Double) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroOptFloat64 {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.double
            return NitroOptFloat64(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroOptFloat64 {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroOptFloat64 = NitroOptFloat64(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toDouble()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Double?) : this(v != null, v ?: 0.0)
    val nullable: Double? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeDouble(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        return out.toByteArray()  // NO 4-byte length prefix
    }

    // --- toJson/fromJson for Map<String, NitroOptFloat64> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroOptBool(val hasValue: Boolean, val value: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroOptBool {
            val hasValue = (buf.get().toInt() != 0)
            val value = (buf.get().toInt() != 0)
            return NitroOptBool(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroOptBool {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroOptBool = NitroOptBool(
                hasValue = map["hasValue"] as Boolean,
                value = map["value"] as Boolean
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(2) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Boolean?) : this(v != null, v ?: false)
    val nullable: Boolean? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeBool(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        return out.toByteArray()  // NO 4-byte length prefix
    }

    // --- toJson/fromJson for Map<String, NitroOptBool> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

/**
 * Contract for the [NitroCamera] module.
 * Implement this in your Kotlin source code.
 * Nitro may call this implementation from any JNI thread.
 * Keep mutable state thread-safe or marshal work onto your own dispatcher.
 */
interface HybridNitroCameraSpec {
    val applicationContext: Context get() = NitroCameraJniBridge.applicationContext
    val activity: Activity? get() = NitroCameraJniBridge.activity

    // Optional lifecycle hooks — override only what you need.
    fun onAttached() {}
    fun onDetached() {}
    fun onActivityAttached(activity: Activity) {}
    fun onActivityDetached() {}

    // source: nitro_camera.native.dart:350
    suspend fun requestCameraPermission(): Long
    // source: nitro_camera.native.dart:353
    fun getCameraPermissionStatus(): Long
    // source: nitro_camera.native.dart:358
    suspend fun requestMicrophonePermission(): Long
    // source: nitro_camera.native.dart:361
    fun getMicrophonePermissionStatus(): Long
    // source: nitro_camera.native.dart:366
    suspend fun getAvailableCameraDevicesJson(): String
    // source: nitro_camera.native.dart:369
    fun getAvailableCameraDevices(): List<CameraDevice>
    // source: nitro_camera.native.dart:372
    fun getDeviceCount(): Long
    // source: nitro_camera.native.dart:373
    fun getDevice(index: Long): CameraDevice
    // source: nitro_camera.native.dart:389
    suspend fun openCamera(deviceId: String, width: Long, height: Long, fps: Long, enableAudio: Long): Long
    // source: nitro_camera.native.dart:398
    suspend fun closeCamera(textureId: Long): Unit
    // source: nitro_camera.native.dart:401
    fun startPreview(textureId: Long): Unit
    // source: nitro_camera.native.dart:404
    fun stopPreview(textureId: Long): Unit
    // source: nitro_camera.native.dart:409
    fun setZoom(textureId: Long, zoom: Double): Unit
    // source: nitro_camera.native.dart:412
    fun setFocusPoint(textureId: Long, x: Double, y: Double): Unit
    // source: nitro_camera.native.dart:415
    fun setAutoFocus(textureId: Long, mode: Long): Unit
    // source: nitro_camera.native.dart:418
    fun setExposure(textureId: Long, value: Double): Unit
    // source: nitro_camera.native.dart:421
    fun setFlash(textureId: Long, mode: Long): Unit
    // source: nitro_camera.native.dart:424
    fun setTorch(textureId: Long, enabled: Long): Unit
    // source: nitro_camera.native.dart:428
    fun setWhiteBalance(textureId: Long, temperature: Long): Unit
    // source: nitro_camera.native.dart:431
    fun setHdr(textureId: Long, enabled: Long): Unit
    // source: nitro_camera.native.dart:445
    suspend fun takePhoto(textureId: Long): PhotoResult
    // source: nitro_camera.native.dart:450
    suspend fun startVideoRecording(textureId: Long, outputPath: String, options: RecordingOptions): Unit
    // source: nitro_camera.native.dart:457
    suspend fun stopVideoRecording(textureId: Long): RecordingResult
    // source: nitro_camera.native.dart:460
    fun pauseRecording(textureId: Long): Unit
    // source: nitro_camera.native.dart:463
    fun resumeRecording(textureId: Long): Unit
    // source: nitro_camera.native.dart:466
    fun cancelRecording(textureId: Long): Unit
    // source: nitro_camera.native.dart:476
    fun enableFrameProcessing(textureId: Long, enabled: Long): Unit
    // source: nitro_camera.native.dart:480
    fun setFrameFormat(textureId: Long, format: Long): Unit
    // source: nitro_camera.native.dart:484
    fun setSamplingRate(textureId: Long, samplingRate: Long): Unit
    // source: nitro_camera.native.dart:491
    fun setFilterShader(textureId: Long, shaderSource: String): Unit
    // source: nitro_camera.native.dart:496
    fun updateOverlay(textureId: Long, overlayData: java.nio.ByteBuffer): Unit
    // source: nitro_camera.native.dart:509
    suspend fun configure(textureId: Long, config: CameraConfig): ResolvedConfig
    // source: nitro_camera.native.dart:513
    fun getSessionStateJson(textureId: Long): String
    // source: nitro_camera.native.dart:516
    fun setVideoStabilization(textureId: Long, mode: Long): Unit
    // source: nitro_camera.native.dart:519
    fun setLowLightBoost(textureId: Long, enabled: Long): Unit
    // source: nitro_camera.native.dart:522
    fun setTorchLevel(textureId: Long, level: Double): Unit
    // source: nitro_camera.native.dart:525
    fun lockExposure(textureId: Long, locked: Long): Unit
    // source: nitro_camera.native.dart:528
    fun lockFocus(textureId: Long, locked: Long): Unit
    // source: nitro_camera.native.dart:531
    fun lockWhiteBalance(textureId: Long, locked: Long): Unit
    // source: nitro_camera.native.dart:534
    fun setTargetOrientation(textureId: Long, degrees: Long): Unit
    // source: nitro_camera.native.dart:539
    fun setDistortionCorrection(textureId: Long, enabled: Long): Unit
    // source: nitro_camera.native.dart:544
    fun enableOrientationEvents(enabled: Long): Unit
    // source: nitro_camera.native.dart:549
    fun enableDeviceAvailabilityEvents(enabled: Long): Unit
    // source: nitro_camera.native.dart:556
    fun setNativeDetector(textureId: Long, detector: String): Unit
    // source: nitro_camera.native.dart:561
    fun getConcurrentCameraIdsJson(): String
    // source: nitro_camera.native.dart:565
    suspend fun takePhotoWithOptions(textureId: Long, options: PhotoOptions): PhotoResult
    // source: nitro_camera.native.dart:570
    suspend fun takeSnapshot(textureId: Long): PhotoResult
    // source: nitro_camera.native.dart:575
    fun reset(): Unit
    val frameStream: Flow<CameraFrame>
    val eventStream: Flow<CameraEvent>
}

@Keep
object NitroCameraJniBridge {
    private val _implementations = java.util.concurrent.ConcurrentHashMap<Long, HybridNitroCameraSpec>()
    private val _idCounter = java.util.concurrent.atomic.AtomicLong(0)
    private var _factory: (() -> HybridNitroCameraSpec)? = null
    private val _asyncExecutor = java.util.concurrent.Executors.newCachedThreadPool()

    lateinit var applicationContext: Context
        private set

    var activity: Activity? = null
        private set

    @JvmStatic external fun initialize(bridgeClass: Class<*>)

    fun registerFactory(factory: () -> HybridNitroCameraSpec, context: Context) {
        applicationContext = context
        _factory = factory
        if (_implementations.isEmpty()) { initialize(this::class.java) }
    }

    @JvmStatic
    fun create_instance_call(key: String): Long {
        val factory = _factory ?: throw IllegalStateException(
            "NitroCamera: no factory registered. Call registerFactory() in onAttachedToEngine().")
        val id = _idCounter.getAndIncrement()
        val impl = factory()
        _implementations[id] = impl
        impl.onAttached()
        return id
    }

    @JvmStatic
    fun destroy_instance_call(instanceId: Long) {
        _implementations.remove(instanceId)?.onDetached()
    }

    fun onActivityAttached(newActivity: Activity) {
        activity = newActivity
        _implementations.values.forEach { it.onActivityAttached(newActivity) }
    }

    fun onActivityDetached() {
        activity = null
        _implementations.values.forEach { it.onActivityDetached() }
    }

    // @NitroNativeAsync helpers — post primitive results via Dart_PostCObject_DL.
    @JvmStatic external fun postNullToPort(dartPort: Long)
    @JvmStatic external fun postInt64ToPort(dartPort: Long, value: Long)
    @JvmStatic external fun postDoubleToPort(dartPort: Long, value: Double)
    @JvmStatic external fun postBoolToPort(dartPort: Long, value: Boolean)
    @JvmStatic external fun postStringToPort(dartPort: Long, value: String)
    // Nullable prim helpers: malloc NitroOptXxx on native heap, post address as kInt64.
    // Dart decodes via Pointer<NitroOptXxx>.fromAddress and frees with malloc.free.
    @JvmStatic external fun postOptInt64ToPort(dartPort: Long, value: Long, hasValue: Boolean)
    @JvmStatic external fun postOptFloat64ToPort(dartPort: Long, value: Double, hasValue: Boolean)
    @JvmStatic external fun postOptBoolToPort(dartPort: Long, value: Boolean, hasValue: Boolean)
    // Record/list-record helper: malloc a [4B len][payload] native buffer, copy
    // value into it, post the address as kInt64. Dart decodes via Pointer<Uint8>.
    // null posts address 0 (nullptr) — NOT Dart_CObject_kNull — matching how
    // nullable record returns are decoded elsewhere (a pointer-typed kInt64,
    // never a bare kNull CObject).
    @JvmStatic external fun postBytesToPort(dartPort: Long, value: ByteArray?)
    // ResolvedConfig NativeAsync return: malloc a native copy via pack_ResolvedConfig_from_jni, post
    // address as kInt64. null posts address 0 — NOT Dart_CObject_kNull — same
    // convention as postBytesToPort above.
    @JvmStatic external fun postResolvedConfigToPort(dartPort: Long, value: ResolvedConfig?)
    // Writes a thrown exception's name/message into the fresh-per-call
    // NitroError* whose address Dart passed as errPtr, before the trampoline
    // still posts exactly one message so the port fires either way.
    @JvmStatic external fun reportNativeAsyncError(errPtr: Long, name: String, message: String)

    // source: nitro_camera.native.dart:350
    @JvmStatic fun requestCameraPermission_call(instanceId: Long, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.requestCameraPermission() }
            postInt64ToPort(dartPort, result.toLong())
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_camera.native.dart:353
    @JvmStatic fun getCameraPermissionStatus_call(instanceId: Long): Long {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        return impl.getCameraPermissionStatus()
    }
    // source: nitro_camera.native.dart:358
    @JvmStatic fun requestMicrophonePermission_call(instanceId: Long, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.requestMicrophonePermission() }
            postInt64ToPort(dartPort, result.toLong())
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_camera.native.dart:361
    @JvmStatic fun getMicrophonePermissionStatus_call(instanceId: Long): Long {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        return impl.getMicrophonePermissionStatus()
    }
    // source: nitro_camera.native.dart:366
    @JvmStatic fun getAvailableCameraDevicesJson_call(instanceId: Long, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.getAvailableCameraDevicesJson() }
            postStringToPort(dartPort, result)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_camera.native.dart:369
    @JvmStatic fun getAvailableCameraDevices_call(instanceId: Long): ByteArray {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        val result = impl.getAvailableCameraDevices()
        val itemBufs = ArrayList<ByteArray>(result.size)
        val tmpBuf = java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        for (item in result) {
            val tmpOut = java.io.ByteArrayOutputStream(168)
            item.writeFieldsTo(tmpOut, tmpBuf)
            itemBufs.add(tmpOut.toByteArray())
        }
        // payload = [4B count][8B × n offsets][item bytes...]
        var offsetPos = 4 + 8L * result.size  // start of item data in payload
        val offsets = LongArray(result.size)
        for (i in result.indices) { offsets[i] = offsetPos; offsetPos += itemBufs[i].size }
        val payloadBuf = java.nio.ByteBuffer.allocate(offsetPos.toInt()).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        payloadBuf.putInt(result.size)
        offsets.forEach { payloadBuf.putLong(it) }
        itemBufs.forEach { payloadBuf.put(it) }
        val payload = payloadBuf.array()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }
    // source: nitro_camera.native.dart:372
    @JvmStatic fun getDeviceCount_call(instanceId: Long): Long {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        return impl.getDeviceCount()
    }
    // source: nitro_camera.native.dart:373
    @JvmStatic fun getDevice_call(instanceId: Long, index: Long): ByteArray {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        val result = impl.getDevice(index)
        return result.encode()
    }
    // source: nitro_camera.native.dart:389
    @JvmStatic fun openCamera_call(instanceId: Long, deviceId: String, width: Long, height: Long, fps: Long, enableAudio: Long, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.openCamera(deviceId, width, height, fps, enableAudio) }
            postInt64ToPort(dartPort, result.toLong())
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_camera.native.dart:398
    @JvmStatic fun closeCamera_call(instanceId: Long, textureId: Long, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            runBlocking { impl.closeCamera(textureId) }
            postNullToPort(dartPort)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_camera.native.dart:401
    @JvmStatic fun startPreview_call(instanceId: Long, textureId: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.startPreview(textureId)
    }
    // source: nitro_camera.native.dart:404
    @JvmStatic fun stopPreview_call(instanceId: Long, textureId: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.stopPreview(textureId)
    }
    // source: nitro_camera.native.dart:409
    @JvmStatic fun setZoom_call(instanceId: Long, textureId: Long, zoom: Double): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.setZoom(textureId, zoom)
    }
    // source: nitro_camera.native.dart:412
    @JvmStatic fun setFocusPoint_call(instanceId: Long, textureId: Long, x: Double, y: Double): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.setFocusPoint(textureId, x, y)
    }
    // source: nitro_camera.native.dart:415
    @JvmStatic fun setAutoFocus_call(instanceId: Long, textureId: Long, mode: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.setAutoFocus(textureId, mode)
    }
    // source: nitro_camera.native.dart:418
    @JvmStatic fun setExposure_call(instanceId: Long, textureId: Long, value: Double): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.setExposure(textureId, value)
    }
    // source: nitro_camera.native.dart:421
    @JvmStatic fun setFlash_call(instanceId: Long, textureId: Long, mode: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.setFlash(textureId, mode)
    }
    // source: nitro_camera.native.dart:424
    @JvmStatic fun setTorch_call(instanceId: Long, textureId: Long, enabled: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.setTorch(textureId, enabled)
    }
    // source: nitro_camera.native.dart:428
    @JvmStatic fun setWhiteBalance_call(instanceId: Long, textureId: Long, temperature: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.setWhiteBalance(textureId, temperature)
    }
    // source: nitro_camera.native.dart:431
    @JvmStatic fun setHdr_call(instanceId: Long, textureId: Long, enabled: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.setHdr(textureId, enabled)
    }
    // source: nitro_camera.native.dart:445
    @JvmStatic fun takePhoto_call(instanceId: Long, textureId: Long, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.takePhoto(textureId) }
            val _bytes = result.encode()
            postBytesToPort(dartPort, _bytes)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_camera.native.dart:450
    @JvmStatic fun startVideoRecording_call(instanceId: Long, textureId: Long, outputPath: String, options: RecordingOptions, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            runBlocking { impl.startVideoRecording(textureId, outputPath, options) }
            postNullToPort(dartPort)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_camera.native.dart:457
    @JvmStatic fun stopVideoRecording_call(instanceId: Long, textureId: Long, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.stopVideoRecording(textureId) }
            val _bytes = result.encode()
            postBytesToPort(dartPort, _bytes)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_camera.native.dart:460
    @JvmStatic fun pauseRecording_call(instanceId: Long, textureId: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.pauseRecording(textureId)
    }
    // source: nitro_camera.native.dart:463
    @JvmStatic fun resumeRecording_call(instanceId: Long, textureId: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.resumeRecording(textureId)
    }
    // source: nitro_camera.native.dart:466
    @JvmStatic fun cancelRecording_call(instanceId: Long, textureId: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.cancelRecording(textureId)
    }
    // source: nitro_camera.native.dart:476
    @JvmStatic fun enableFrameProcessing_call(instanceId: Long, textureId: Long, enabled: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.enableFrameProcessing(textureId, enabled)
    }
    // source: nitro_camera.native.dart:480
    @JvmStatic fun setFrameFormat_call(instanceId: Long, textureId: Long, format: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.setFrameFormat(textureId, format)
    }
    // source: nitro_camera.native.dart:484
    @JvmStatic fun setSamplingRate_call(instanceId: Long, textureId: Long, samplingRate: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.setSamplingRate(textureId, samplingRate)
    }
    // source: nitro_camera.native.dart:491
    @JvmStatic fun setFilterShader_call(instanceId: Long, textureId: Long, shaderSource: String): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.setFilterShader(textureId, shaderSource)
    }
    // source: nitro_camera.native.dart:496
    @JvmStatic fun updateOverlay_call(instanceId: Long, textureId: Long, overlayData: java.nio.ByteBuffer): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.updateOverlay(textureId, overlayData)
    }
    // source: nitro_camera.native.dart:509
    @JvmStatic fun configure_call(instanceId: Long, textureId: Long, config: CameraConfig, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.configure(textureId, config) }
            postResolvedConfigToPort(dartPort, result)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_camera.native.dart:513
    @JvmStatic fun getSessionStateJson_call(instanceId: Long, textureId: Long): String {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        return impl.getSessionStateJson(textureId)
    }
    // source: nitro_camera.native.dart:516
    @JvmStatic fun setVideoStabilization_call(instanceId: Long, textureId: Long, mode: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.setVideoStabilization(textureId, mode)
    }
    // source: nitro_camera.native.dart:519
    @JvmStatic fun setLowLightBoost_call(instanceId: Long, textureId: Long, enabled: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.setLowLightBoost(textureId, enabled)
    }
    // source: nitro_camera.native.dart:522
    @JvmStatic fun setTorchLevel_call(instanceId: Long, textureId: Long, level: Double): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.setTorchLevel(textureId, level)
    }
    // source: nitro_camera.native.dart:525
    @JvmStatic fun lockExposure_call(instanceId: Long, textureId: Long, locked: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.lockExposure(textureId, locked)
    }
    // source: nitro_camera.native.dart:528
    @JvmStatic fun lockFocus_call(instanceId: Long, textureId: Long, locked: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.lockFocus(textureId, locked)
    }
    // source: nitro_camera.native.dart:531
    @JvmStatic fun lockWhiteBalance_call(instanceId: Long, textureId: Long, locked: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.lockWhiteBalance(textureId, locked)
    }
    // source: nitro_camera.native.dart:534
    @JvmStatic fun setTargetOrientation_call(instanceId: Long, textureId: Long, degrees: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.setTargetOrientation(textureId, degrees)
    }
    // source: nitro_camera.native.dart:539
    @JvmStatic fun setDistortionCorrection_call(instanceId: Long, textureId: Long, enabled: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.setDistortionCorrection(textureId, enabled)
    }
    // source: nitro_camera.native.dart:544
    @JvmStatic fun enableOrientationEvents_call(instanceId: Long, enabled: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.enableOrientationEvents(enabled)
    }
    // source: nitro_camera.native.dart:549
    @JvmStatic fun enableDeviceAvailabilityEvents_call(instanceId: Long, enabled: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.enableDeviceAvailabilityEvents(enabled)
    }
    // source: nitro_camera.native.dart:556
    @JvmStatic fun setNativeDetector_call(instanceId: Long, textureId: Long, detector: String): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.setNativeDetector(textureId, detector)
    }
    // source: nitro_camera.native.dart:561
    @JvmStatic fun getConcurrentCameraIdsJson_call(instanceId: Long): String {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        return impl.getConcurrentCameraIdsJson()
    }
    // source: nitro_camera.native.dart:565
    @JvmStatic fun takePhotoWithOptions_call(instanceId: Long, textureId: Long, options: PhotoOptions, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.takePhotoWithOptions(textureId, options) }
            val _bytes = result.encode()
            postBytesToPort(dartPort, _bytes)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_camera.native.dart:570
    @JvmStatic fun takeSnapshot_call(instanceId: Long, textureId: Long, errPtr: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: run {
            reportNativeAsyncError(errPtr, "IllegalStateException", "No implementation registered for instance")
            postNullToPort(dartPort)
            return
        }
        _asyncExecutor.execute {
            try {
            val result = runBlocking { impl.takeSnapshot(textureId) }
            val _bytes = result.encode()
            postBytesToPort(dartPort, _bytes)
            } catch (e: Throwable) {
                reportNativeAsyncError(errPtr, e.javaClass.simpleName, e.message ?: "An unknown native exception occurred.")
                postNullToPort(dartPort)
            }
        }
    }
    // source: nitro_camera.native.dart:575
    @JvmStatic fun reset_call(instanceId: Long): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroCamera instance $instanceId not registered")
        impl.reset()
    }
    private val _streamJobs = java.util.concurrent.ConcurrentHashMap<Pair<String, Long>, kotlinx.coroutines.Job>()

    @JvmStatic external fun emit_frameStream(dartPort: Long, item: CameraFrame): Boolean

    @JvmStatic fun nitro_camera_register_frame_stream_stream_call(instanceId: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: return
        _streamJobs[Pair("frameStream", dartPort)] = CoroutineScope(Dispatchers.Default).launch(start = CoroutineStart.UNDISPATCHED) {
            impl.frameStream.collect { item -> 
                if (!emit_frameStream(dartPort, item)) {
                    _streamJobs.remove(Pair("frameStream", dartPort))?.cancel()
                    return@collect
                }
            }
        }
    }
    @JvmStatic fun nitro_camera_release_frame_stream_stream_call(dartPort: Long) {
        _streamJobs.remove(Pair("frameStream", dartPort))?.cancel()
    }
    @JvmStatic external fun emit_eventStream(dartPort: Long, item: ByteArray): Boolean

    @JvmStatic fun nitro_camera_register_event_stream_stream_call(instanceId: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: return
        _streamJobs[Pair("eventStream", dartPort)] = CoroutineScope(Dispatchers.Default).launch(start = CoroutineStart.UNDISPATCHED) {
            impl.eventStream
                .buffer(capacity = 64, onBufferOverflow = kotlinx.coroutines.channels.BufferOverflow.DROP_OLDEST)
                .collect { item ->
                    if (!emit_eventStream(dartPort, item.encode())) {
                        _streamJobs.remove(Pair("eventStream", dartPort))?.cancel()
                        return@collect
                    }
                }
        }
    }
    @JvmStatic fun nitro_camera_release_event_stream_stream_call(dartPort: Long) {
        _streamJobs.remove(Pair("eventStream", dartPort))?.cancel()
    }
}
